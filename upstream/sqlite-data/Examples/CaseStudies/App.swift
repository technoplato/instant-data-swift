import SQLiteData
import SwiftUI

@main
struct CaseStudiesApp: App {
  var body: some Scene {
    WindowGroup {
      Form {
        Text(
          """
          Open the preview in each case study file to run a case study.
          """
        )
      }
    }
  }
}

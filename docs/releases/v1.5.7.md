# Instant Swift Data 1.5.7

Status: release candidate. The official workflow must not create `v1.5.7` until every requirement below is green on the release commit.

## Release focus

This release consolidates the current memory, reconnect, outbox, live-query, and Scribe compatibility work; restores the cross-runtime exercise gym on top of current `main`; pins the upstream TypeScript reference as a Git submodule; and changes benchmark policy from advisory reporting to fail-closed publication enforcement.

## Correctness contract

- Local writes complete after local materialization and durable outbox persistence, never after an implicit cloud wait.
- Offline writes survive relaunch, drain in order, and preserve newer optimistic state during server refresh.
- Rejection is isolated per mutation/query/stream/media item.
- Rapid open-segment rewrites remain monotonic through finalization.
- Swift→TypeScript and TypeScript→Swift lanes must report zero lost, duplicate, reordered, or finalized-regression updates.
- Linked and infinite-query fixtures must preserve bounded result cardinality and deterministic result counts.
- Entity delivery and progressive audio/media transfer remain independent.

## Performance contract

Run:

```bash
validation/run-performance-gate.sh deterministic
validation/run-performance-gate.sh live
```

The official release workflow runs the live command and creates the tag only afterward. Default policy requires:

- Swift p50 latency no slower than the pinned TypeScript core.
- Swift p95 latency no slower than the pinned TypeScript core.
- Swift throughput at least equal to TypeScript for equivalent work.
- Scribe-shaped physical-footprint and retained-query memory tests to pass.
- Durable enqueue, offline restore, and reconnect drain to meet the same ratio policy.
- Compiler/runtime warnings to remain at zero.
- Every correctness and performance comparison to emit zero failures.

## Reproducibility

- TypeScript reference: `upstream/instant` submodule at `e71017612aed4031710a35e2fcace30d38d557ac`.
- TypeScript SDK contract: `@instantdb/core` / admin `1.0.49`.
- Node: 22 in GitHub Actions.
- Swift: release configuration on `macos-15`.
- Evidence: uploaded as the `instant-data-v1.5.7-evidence` workflow artifact before tag creation.

## Publication rule

`v1.5.7` is publishable only through `.github/workflows/release-gate.yml`. The workflow validates this document, runs `validation/run-performance-gate.sh live`, uploads evidence, then and only then creates the tag and GitHub release.

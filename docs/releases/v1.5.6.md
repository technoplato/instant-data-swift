## 1.5.6 — Production Scribe namespace soak + dual Instant thrash gate

### Problem

Release gate 1.5.5 still soaked against `linked_infinite_*` toy namespaces and a
counter-only dual-write thrash driver. Field multi‑GB idle on iPad (home screen,
not recording) used production namespaces (`recordings`, `transcriptionWords`,
…) and a **second Instant `debugLogs` store** with multi-attr batches. Releases
could pass while that thrash class was still un-gated.

### Library

- `ScribeProductionShapedSchema` — production entity names + multi-attr
  `debugLogs` fixture matching InstantDBLogger ops.
- `ScribeShapedAuthenticatedIdleMemorySoakTests` — guest auth always; live guest
  when credentials present; boots second InstantRuntime for debugLogs thrash.
- `LinkedInfiniteScribeShapedMemorySoakTests` — seed/page on production namespaces.
- `InstantDiagnosticFeedbackLoopTests` — demotion suite uses production
  namespaces + guest auth + dual Instant thrash proof (not TodoExample alone).
- `validation/verify-scribe-shaped-memory-soak.sh` — sequential suites; auto live
  auth from `SCRIBE_MAIN_INSTANT_APP_ID` / `~/.config/instant-tools/scribe-main.env`.

Absolute idle product fail remains **>400 MiB** physical footprint (target ≤250 MiB).

### Host (Scribe)

Pin `exact: "1.5.6"`. Keep Instant debugLogs dual-write **opt-in**
(`SCRIBE_LOGS_INSTANT_APP_ID`); Instant-lane filter for library chatter (Scribe
`InstantDBLogger`) still recommended when Instant lane is enabled.

### Gate

```bash
validation/verify-scribe-shaped-memory-soak.sh
```

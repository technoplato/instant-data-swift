# 2026-08-12 live revision cardinality

- owner: `grok-4.6-live-revision`
- plan: Instant `2026-08-12-live-revision-cardinality`
- mode: mower
- issues: #044, #155
- split: Instant-library characterization only. Does not steal `codex-desktop/019fe994` InstantStore/Runtime claims or Scribe recording sources.
- intent: Replicate the 4 GB recording balloon as 10,000 same-entity puts whose domain cardinality stays 1 while unbounded namespace observation rematerializes every sibling. User invariant: do not debounce or drop live sync revisions; prune the operation journal after ack; observe live current state plus bounded finalized pages.
- 2026-08-12T12:36:00-0400 user-explicit-allow steal: `grok-4.6-live-revision` may steal InstantStore/Runtime ownership for the live-put rematerialize memory slice only. Steal is allowed only because the user explicitly allowed it at this timestamp. Prior `codex-desktop/019fe994` ids remain. Scope: Swift-only memory-byte tests first, then skip rematerialize of observers whose result does not include the changed entity. Do not edit outbox, deferred values, or bounded-query candidate enumeration. InstantRuntime claimed; no Runtime edit unless InstantStore publish contract requires it.
- 2026-08-12T13:07:02-0400 splice + include skip: `shouldRefresh` intersects plan+include namespaces; `splicedEmission` replaces lastValues in place when membership and order keys are unchanged. Diagnostics: skippedObserverCount, splicedObserverCount, rematerializedObserverCount, materializedSnapshotCount. Autoresearch `2026-08-12-live-put-observation-memory`. InstantRuntime still unedited.
- 2026-08-12T13:52:11-0400 trial 2: user goal Instant physical footprint ~65 MB (SQLiteData parity) with realtime Instant sync on; fail-fast 125 MB. Do not debounce live revisions. InstantRuntime still unedited. Next bite is Scribe opt-in of already-shipped deferred residency for `transcriptionSegments/text` and `transcriptionSegments/wordsJSON` (non-indexed payload strings). Library harness measures hot-store payload bytes vs a 1,000-row corpus. InstantInfiniteQuery.swift remains with prior frozen owners; this trial does not edit it.
- 2026-08-12T16:10:00-0400 trial 3: user said "keep going then" toward ~65 MB. Mac idle after trial 2 was still ~277 MB physical / 423 MB peak. InstantRuntime still unedited. InstantInfiniteQuery still not edited (Mac live path already uses page-sized chunks). Bite is `SQLitePersistenceStore.loadStoreSnapshotWithoutTransaction`: when `instant_live_query_triples` has rows, load only those entity IDs plus `instant_outbox_effect_entities` into InstantStore. Matches TypeScript `Reactor.js` per-query `result.store` from `createStore(attrs, result.triples)` plus pending mutations, and SQLite Data disk+SQL LIMIT. Empty watermark query results (zero triple rows) keep the full load so cache-residency tests stay valid. Diff-save of a thin snapshot must not delete unobserved SQLite triples. Prior `codex-desktop/019fe994` SQLitePersistenceStore ids remain; this is an append-only claim for the scoped bootstrap load.
- 2026-08-12T17:25:00-0400 trial 4: dirty Mac soak of trial 3 KEEP measured phys 271 MB idle, 442 MB at ~2 min recording, peak 489 MB (pid 52693). Live malloc ~86 MB; Instant objects ~10-20 MB. Instant sqlite 44 MB, 345 segments, 92 live query keys / 5.5 MB result JSON covering 372 of 426 entities — scoped bootstrap ≈ full corpus because stale infinite-query pages stay persisted. TypeScript `Reactor.js` `_cleanupQuery` calls `querySubs.unloadKey(hash)` when a query has no listeners, even while `pendingMutations` exist. Swift `pruneLiveQueryResults` unions every query key into the protected set whenever the outbox has an optimistic overlay, so session prune never unloads inactive pages during live speech. Bite: when `preservingQueryKeys` is non-empty, unload keys that are not in that set (do not protect-all for outbox). Bootstrap with empty preserving keys keeps today's age/count + conservative-all. InstantRuntime unedited.
- 2026-08-12T17:58:00-0400 trial 5: iPad overlay while recording 74-92 MB physical / peak 191 (SQLiteData 55.7, goal ~65, 125 current PASS). Trial 4 sqlite 92→4 query keys. Remaining recordings list querySub has 481 segments (max 328 on one parent) despite nested limit 2. Bite: apply nested include limits when saving/loading live query triples in SQLitePersistenceStore. InstantRuntime unedited. No debounce.

# 019fe994 memory root

- owner: `codex-desktop/019fe994-1250-7b42-825e-9b75836d9173/root`
- plan: `2026-08-09-bounded-intent-outbox-memory`
- mode: mower
- scope: row-addressed outbox lifecycle, bounded delivery hydration, receive-loop independence
- worker: `codex-desktop/019fe994-1250-7b42-825e-9b75836d9173/root/instant_bounded_outbox`
- split: worker owns bounded durable delivery selection in `SQLitePersistenceStore.swift`, `InstantRuntime.swift`, new `BoundedOutboxDelivery.swift`, and new focused tests; worker does not touch `OutboxSameEntitySupersession.swift` or its untracked claim
- scope-addition: root authorized the worker to update `Sources/InstantSwiftData/InstantSwiftData.swift` and focused `MutationDeliveryTests.swift` so wait-all reads a durable body-free summary instead of the bounded resident claim actor
- conflict: prior `2026-08-09-outbox-wire-hydration` claims are preserved; that agent is not live in the current agent tree
- supersession-slice: root reassigned this worker to conservative immediate durable-tail same-entity supersession. Worker now owns `OutboxSameEntitySupersession.swift`, the existing `SQLitePersistenceStore.swift` / `InstantRuntime.swift` claims, and new focused `InstantOutboxSupersessionIntegrationTests.swift`; the legacy untracked `grok-build` supersession marker is preserved and its agent is not live.
- supersession-finish: root reassigned the incomplete uncommitted slice from `instant_bounded_outbox` to `instant_supersession_finish`; the finisher owns those same paths, preserves the prior worker's edits, and will freeze the reviewed/tested diff without committing.
- supersession-policy-retirement: root expanded the finisher scope to retire the unsafe queue-wide projection and update `Outbox.swift`, `OpenSegmentWriteRecipe.swift`, the legacy policy tests, and ADR 0015 follow-on/open-segment documents; the exact immediate-tail implementation remains authoritative.
2026-08-10T10:24:00-0400 root resumed steps 5-6: terminal mutation errors become body-free idempotent duplicates with no live-query remove/add resubscription, followed by addressed affected-key rejection/rebase. Claims remain under plan 2026-08-09-bounded-intent-outbox-memory.
- 2026-08-10T11:43:27-0400 rejection-fast-path: `scribe_pipeline_write_shape_review` owns `InstantOptimisticEffectFootprint.swift`, `SQLitePersistenceStore.swift`, `InstantRuntime.swift`, and new `InstantTerminalFailureComponentTests.swift`. Scope is the bounded affected-component terminal rejection design: v1 forward+rollback footprint metadata, row revisions, bounded normalization/component load, exact claim/status/revision closure CAS, targeted entity persistence, and peel/replay under the operation gate. Prior owners are no longer live on these paths; preserve all landed and untracked work. No Scribe edits and no commit.
- 2026-08-10T12:10:00-0400 typed-selection-prerequisite: root owns `Sources/InstantSwiftData/InstantTypedAPI.swift` and `Tests/InstantSwiftDataTests/TypedAPITests.swift`. Scope is a reusable type-erased, schema-safe array selection for more than six heterogeneous fields in one bounded query, required to exclude legacy route blobs from ordinary Scribe list/detail state before `recordingRouteChunks` writes land. This is additive Instant API work only; preserve the frozen terminal-rejection WIP.
- 2026-08-10T13:45:41-0400 deferred-value-residency: `instant_deferred_values` owns `Sources/InstantSwiftDataCore/DeferredValueResidency.swift`, `InstantStore.swift`, `TripleIndexes.swift`, the narrow bootstrap/config additions in `InstantRuntime.swift` and `Sources/InstantSwiftData/InstantSwiftData.swift`, only necessary non-overlapping sections of `SQLitePersistenceStore.swift`, and `Tests/InstantSwiftDataCoreTests/DeferredValueResidencyTests.swift`. Scope is explicit attribute-id residency configuration, eligible-attribute validation, bootstrap without deferred payload decode into hot indexes, selection-scoped page hydration, and exact optimistic update/restart/rejection. Preserve every existing dirty edit; coordinate around the terminal-rejection worker's untracked `InstantOptimisticEffectFootprint.swift`, SQLite/runtime WIP, terminal component tests, and root's typed-selection changes. No commit.
- 2026-08-10T14:16:53-0400 terminal-component-reassignment: root reassigned the frozen terminal-rejection slice to `instant_deferred_values`. This agent now owns `InstantOptimisticEffectFootprint.swift`, `InstantTerminalFailureComponentTests.swift`, and terminal-component-only sections of `SQLitePersistenceStore.swift` and `InstantRuntime.swift`: bounded 50-body/8-MiB closure load, metadata normalization, exact claim/status/store+row-revision closure compare-and-swap, targeted persistence, and live claimed-rejection wiring. Preserve the completed deferred-residency slice and every concurrent supersession, outbox, typed-selection, and Scribe edit. No commit and no SwiftPM until root releases the serialized slot.
- 2026-08-10T15:33:43-0400 deferred-infinite-query-follow-up: root authorized `instant_deferred_values` to own `InstantInfiniteQuery.swift`, the minimum deferred-hydration callback section of `InstantRuntime.swift`, focused additions to `DeferredValueResidencyTests.swift` and `InstantInfiniteQueryParityTests.swift`. Scope is slice-before-hydration for local infinite queries, incremental page hydration where the raw sequence is unchanged, exact reordering on new emissions, and one typed persistence failure snapshot before termination. Preserve all concurrent source and test edits. Source/parser/diff checks only until root releases SwiftPM.
- 2026-08-10T15:49:00-0400 deferred-infinite-query-source-freeze: implementation is source-frozen. `InstantInfiniteQuery.swift` now observes the unbounded local identity/order result directly from the hot store, slices before SQLite hydration, caches only visible hydrated snapshots, reads newly visible IDs on same-sequence page advance, invalidates/reloads the visible window on a new sequence, and terminates after one typed failure snapshot. Live starter/chunk callbacks terminate through the same typed failure shape. `InstantRuntime.swift` changes are confined to the optional `observeLiveInfiniteQueryChunk`/`hydratingDeferredValues` failure callback and shared failure normalization/reporting at the deferred hydration section. `DeferredValueResidencyTests.swift` adds selected-page, route-free, reorder, and failure contracts. `swiftc -parse` and diff whitespace checks passed; SwiftPM remains unrun pending root release.
2026-08-10T19:39:00Z root resumes the frozen `instant_bounded_outbox` test path as mower under plan `2026-08-09-bounded-intent-outbox-memory`. Split: root owns the explicit-flush terminal-rejection regression and later Runtime integration; `instant_deferred_values` owns infinite-query hydration and only its minimal Runtime callback. Root will wait for that source freeze before editing Runtime.
- 2026-08-10T15:41:20-0400 terminal-component-index-frontier: root delegated `terminal_component_index_frontier` to replace the terminal component's later-row metadata scan with an indexed entity frontier plus a separately bounded global frontier. This worker owns only the component-resolution helpers in `SQLitePersistenceStore.swift` and focused `InstantTerminalFailureComponentTests.swift`, preserves the root's new footprint precondition/rule test, and will freeze source before requesting the serialized SwiftPM lane.
- 2026-08-10T16:07:00-0400 terminal-component-index-frontier-source-freeze: test-first source is frozen. The 10,000-row disjoint-tail test now requires exact scalar metadata counts (one target outbox row plus one target effect-entity row per load/commit proof), and a new global-successor test preserves conservative all-active closure. Component resolution probes unknown/global rows through their dedicated indexes, then walks only reachable entity edges through `instant_outbox_effect_entities_lookup_idx`; every component path stops after at most 51 bodies or once encoded bytes prove more than 8 MiB. `swiftc -parse`, `git diff --check`, and synthetic SQLite `EXPLAIN QUERY PLAN` checks passed; SwiftPM is pending root's serialized lane release.
- 2026-08-10T15:48:03-0400 deferred-infinite-query-failure-test-freeze: all four new infinite-query tests now install immediate deferred subscription cleanup. The failure contract corrupts only `instant_triples.json` for the configured deferred row after runtime bootstrap and before subscription, preserving valid hot-store identity/order while deterministically forcing `loadDeferredValues` decoding to fail. The reconnecting connection-close simulation was removed. Focused `swiftc -parse`, tracked diff whitespace, and untracked test-file whitespace checks passed; no SwiftPM command was run.
- 2026-08-10T15:52:04-0400 deferred-infinite-query-known-diagnostic-freeze: the deterministic hydration-failure test now scopes subscription, typed failure snapshot, and termination assertions inside `withKnownIssue`, matching the exact deferred-hydration diagnostic prefix while retaining immediate deferred unsubscription. Focused parser and whitespace checks passed; production source is unchanged and SwiftPM remains unrun by this worker.
- 2026-08-10T15:50:40-0400 terminal-component-index-frontier-verified: `swift test --jobs 1 --no-parallel --filter InstantTerminalFailureComponentTests` passed all 12 tests in 1.189 seconds after a full package rebuild. The exact 10,000-row contract passed in 1.050 seconds with one decoded body and scalar metadata fixed at one target outbox row plus one target effect-entity row for load, then cumulative two plus two for the exact commit reproof. Transitive failed-active, global conservative closure, 51-body fail-closed, 8-MiB fail-closed, normalization, claim, and closure compare-and-swap tests all passed. Source is frozen for root integration; no commit was made.
- 2026-08-10T16:06:59-0400 deferred-query-revision-and-live-bootstrap: root authorized `instant_deferred_values` to own `InstantInfiniteQuery.swift`, the deferred-hydration section of `InstantRuntime.swift`, a narrow `InstantStore.swift` current-sequence accessor if required, `DeferredValueResidencyTests.swift`, and focused infinite-query tests. Test-first scope: hydrate only under the operation gate when the store sequence still matches the emission, drop/retry stale emissions, and make stalled pre-bootstrap live paging expand payload-free identity/order rows while hydrating only newly visible IDs. Preserve all other dirty edits; parser/diff checks only until the serialized SwiftPM slot is explicitly released.
- 2026-08-10T16:15:52-0400 deferred-query-revision-and-live-bootstrap-source-freeze: source is frozen after test-first implementation. `InstantStore.swift` adds only a current-sequence accessor. `InstantRuntime.swift` changes are confined to one post-persistence/pre-publication test hook, gate+sequence-checked deferred emission/snapshot hydration, and payload-free local infinite identity materialization. `InstantInfiniteQuery.swift` drops stale local hydration requests, retries stale pre-bootstrap expansion, and caches hydrated visible IDs so each stalled-live page reads only its newly exposed payloads. `DeferredValueResidencyTests.swift` adds the deterministic SQLite-committed/hot-store-unpublished page-advance race and a six-row stalled-live three-page decode-count contract. Focused `swiftc -parse` and tracked/untracked whitespace checks passed; SwiftPM remains unrun pending root's explicit serialized-slot release.
- 2026-08-10T16:06:32-0400 terminal-component-P1-follow-up: root delegated reviewer-confirmed P1s to `terminal_component_index_frontier`. This worker owns migration 0015 and the entity-frontier ordering key/index/write path in `SQLitePersistenceStore.swift`, compact-shell removal/replacement semantics in `Outbox.swift` plus the bounded terminal Runtime block only, focused terminal component tests, and the explicit 10,000-row rejection regression in `InstantBoundedOutboxDeliveryTests.swift`. Goals are zero frontier sort/full-scan work with a 51-row cap and zero compact resident shells after repeated terminal rejection. Preserve all concurrent edits; parser/diff freeze before requesting SwiftPM.
- 2026-08-10T16:11:52-0400 terminal-component-P1-source-freeze: migration 0015 adds/backfills `created_at_ms` on effect-entity rows and replaces the lookup index with `(entity_id, created_at_ms, mutation_id)`; every effect-footprint replacement writes the ordering key. The entity frontier records exact SQLite sort/full-scan statement counters and maximum returned rows. Focused tests require a 10,000-row same-entity tail to stop at 51 scalar outbox rows, 50 effect rows, zero decoded bodies, zero sort operations, and zero full-scan steps; migration backfill/index order is also covered. Terminal commit now removes the failed resident shell and refreshes successors only if already present; the explicit repeated 10,000-row rejection test requires the actor to stay empty. All five owned files pass `swiftc -parse`; `git diff --check` passes. Source is frozen pending root's serialized SwiftPM release.
- 2026-08-10T16:13:26-0400 terminal-component-P1-fixture-refreeze: the pre-bounded-delivery schema fixture now also removes the `0015_outbox_effect_entity_order` migration marker, ensuring legacy bootstrap exercises the new column backfill and covering-index creation. All five owned files still pass `swiftc -parse`; `git diff --check` passes. Source remains frozen and SwiftPM remains unrun pending root release.
- 2026-08-10T16:18:06-0400 terminal-component-P1-first-build-fix: the released focused build stopped before tests because four new assertions awaited actor methods inside synchronous `expectNoDifference` autoclosures (`InstantTerminalFailureComponentTests.swift:233`; `InstantBoundedOutboxDeliveryTests.swift:1918,1929,1931`). Only those awaits were moved into local values. All five owned files pass `swiftc -parse`; `git diff --check` passes. Source is refrozen and SwiftPM remains stopped pending a new serialized-lane release.
- 2026-08-10T16:21:43-0400 terminal-component-P1-verified: `swift test --jobs 1 --no-parallel --filter InstantTerminalFailureComponentTests` passed all 14 tests in 2.137 seconds after a 146.73-second coherent package rebuild. The 10,000-row same-entity covered-frontier regression passed in 0.987 seconds and the 0015 migration backfill/index regression passed in 0.021 seconds. `swift test --jobs 1 --no-parallel --filter repeatedExplicitFailureOfTenThousandRowsKeepsResidentActorEmpty` passed its single test in 34.609 seconds, proving two terminal failures leave the resident outbox actor empty. Source remains frozen; no commit was made; the serialized SwiftPM slot is released.
- 2026-08-10T16:41:11-0400 failed-retry-window: root delegated `retry_window_implementation` to replace connect-time failed-mutation queue hydration with indexed keyset windows of at most 50 bodies and 8 MiB of SQLite blob bytes. This worker owns retry-window-only sections of `SQLitePersistenceStore.swift` and `InstantRuntime.swift`, plus new `InstantFailedMutationRetryWindowTests.swift`; it preserves every terminal-component, deferred-query, delivery, and supersession edit. The slice must use exact row revision/status/overlay compare-and-swap, let later eligible failed rows progress past corrupt or legacy rows, drain 101 eligible rows as 50/50/1 during one healthy connection, patch only resident rows already present, and never fall back to a queue-wide body read. No commit.
- 2026-08-10T16:48:22-0400 infinite-query-retention-window: `route_chunk_integration` owns `InstantInfiniteQuery.swift`, the public infinite-query vocabulary in `InstantSwiftData.swift` and `InstantTypedAPI.swift`, and focused `InstantInfiniteQueryParityTests.swift`, `DeferredValueResidencyTests.swift`, and `TypedAPITests.swift`. Scope is a reusable accumulated-by-default or bounded contiguous page-retention policy, previous-page navigation, live chunk/subscription eviction cleanup, and hydrated-cache pruning. Preserve all deferred-query and typed-selection edits; do not touch `InstantRuntime.swift` or `SQLitePersistenceStore.swift`. Tests first, no commit, and no SwiftPM until the retry worker freezes and root releases the serialized slot.
- 2026-08-10T16:55:06-0400 failed-retry-window-source-freeze: retry source and four focused tests are frozen. `retryAutomaticFailedMutationWindow(after:excludingMutationIDs:maximumBodyCount:maximumEncodedBodyByteCount:)` reads the partial `instant_outbox_failed_retry_window_idx` by `(created_at_ms, mutation_id)` keyset, admits no more than 50 addressed bodies or 8 MiB of actual `length(CAST(json AS BLOB))` bytes, and commits only after exact status/overlay/claim/revision/delivery-state/body proofs; any race returns nil with no partial transition. Corrupt and oversized rows are quarantined, legacy rows lacking applied-overlay proof are durably isolated, and later eligible rows continue. Runtime drains successive windows in one healthy connect and patches the resident actor only with `replaceIfPresent`. The 101-row contract records 50/50/1 decoded windows; the 10,000-row contract requires 50 decodes, zero queue-wide reads, and zero SQLite sort/full-scan steps. All three owned files pass `swiftc -frontend -parse`; owned diff whitespace passes; the partial-index query was separately confirmed as an SQLite range search. No SwiftPM command has run. Awaiting both Instant source freezes and root's serialized SwiftPM release.
- 2026-08-10T16:55:52-0400 failed-retry-window-fixture-expansion: root authorized this worker to touch only `restorePreBoundedDeliveryOutboxSchema` in `InstantBoundedOutboxDeliveryTests.swift`, adding migration 0016 to the marker deletion so the synthetic pre-0012 rebuild cannot drop the retry index while leaving its migration marked applied. No other existing bounded-delivery test ownership changed.
- 2026-08-10T16:56:13-0400 failed-retry-window-fixture-refreeze: `restorePreBoundedDeliveryOutboxSchema` now deletes `0016_failed_mutation_retry_window` together with 0012-0015. Both source files and both owned/focused test files pass `swiftc -frontend -parse`; their diff whitespace check passes. Retry and fixture source are frozen; no SwiftPM command has run, pending the remaining Instant owner freeze and root's serialized release.
- 2026-08-10T16:58:35-0400 failed-retry-window-test-lifetime-refreeze: final static review found the corrupt-row test was reusing the seed actor after its SQLite handle was deliberately closed for external fault injection. The test now closes only `seedPersistence`, corrupts the file, then bootstraps a fresh persistence actor before exercising retry. Focused parser and scoped whitespace checks pass. Source/tests are refrozen; no SwiftPM command has run.
- 2026-08-10T17:16:06-0400 failed-retry-pump-follow-up: root's focused run passed all four retry-window tests in 1.282 seconds, including the 10,000-row contract in 1.154 seconds and the two expected corrupt/legacy diagnostics. Root review then rejected the synchronous connect-time all-window loop because its gate occupancy and startup latency still scale with retry queue depth. This worker resumes the same Runtime/SQLite/retry-test claims to add a body-free eligible EXISTS at connect, move one retry window per turn into the existing coalesced mutation-delivery pump, alternate retry and delivery progress, and reschedule until no retry rows remain. A focused suspended/large-set test must prove connect returns before all retry windows while the same healthy session still drains 101 without delivery starvation. No SwiftPM until a new root release.
- 2026-08-10T17:24:08-0400 failed-retry-pump-source-freeze: follow-up source/tests are frozen. SQLite now exposes `hasAutomaticFailedMutationRetryCandidate(excludingMutationIDs:)`, an indexed `SELECT EXISTS` that reads no body. Connect applies fresh server attributes, runs only that body-free eligibility probe, saves/publishes opened state, starts receiving, and asynchronously requests the existing coalesced pump. Each pump turn acquires the operation gate for exactly one `retryAutomaticFailedMutationWindow(after:nil, ...)`, patches only resident entries already present, publishes status, releases the gate, then claims/sends exactly one bounded delivery window. A continuation result yields before the next retry+delivery turn; exact-row races and local window failures use bounded retry backoff while still allowing one delivery attempt. Direct/manual retry and the package delivery-test entry point retain their behavior. The existing 101 test now waits for asynchronous 50/50/1 completion. A fifth test suspends retry windows one and two: it requires `connect()` to return in 500 ms while window one is uncommitted, requires the first 50 transacts to be sent before window two begins, then requires all 101 failed rows to reach pending on the same live session with zero queue-wide reads. Both source files and the focused retry test pass `swiftc -frontend -parse`; scoped diff whitespace passes. No SwiftPM command has run after this follow-up; awaiting root release.
- 2026-08-10T17:27:00-0400 failed-retry-pump-test-safety-refreeze: the new suspension test's entry waits now use cancellation-aware one-millisecond polling under the five-second test timeout, rather than a checked continuation that could pin a failing timeout. Its 500 ms connect-independence proof has a separate 750 ms failure-only release fence: the old synchronous sweep deterministically fails at 500 ms, then unwinds instead of hanging the serialized suite. The ordinary passing path cancels that fence and keeps windows one and two suspended for the ordering assertions. Parser and scoped whitespace checks remain green; source/test freeze is restored and SwiftPM remains unrun.
- 2026-08-10T17:30:12-0400 failed-retry-pump-verified-and-released: root's exact frozen `swift test --jobs 1 --no-parallel --filter InstantFailedMutationRetryWindowTests` rerun passed 5/5 in 1.343 seconds; the suspended connect/pump ordering test passed in 0.055 seconds, the 10,000-row disjoint test passed in 1.160 seconds, and only the two expected corrupt/legacy IssueReporting diagnostics appeared. Final read-only review confirmed connect does no retry body hydration or retry loop, schedules the pump only after opened state is published and connection gates are released, and the pump releases the operation gate between its single retry window and single delivery window before yielding each continuation. SQLite retry transitions remain exact row-qualified and the connect eligibility probe remains body-free. Scoped diff whitespace is clean. No residual source defect was found; the full package suite was not run, the shared tree remains dirty with unrelated/concurrent work, and all retry work remains uncommitted as requested. This worker releases Runtime/SQLite/retry-test/bounded-fixture ownership to root for server-apply integration.
- 2026-08-10T17:31:09-0400 bounded-server-apply-rebase: root delegated the fundamental server-apply slice to `retry_window_implementation`, which reacquires only the server-apply sections of `InstantRuntime.swift` and `SQLitePersistenceStore.swift` plus new `InstantBoundedServerApplyRebaseTests.swift`. Scope is compact metadata planning, indexed multi-root affected-component discovery, streamed pages of at most 50 bodies / 8 MiB, exact store/row/closure compare-and-swap, targeted persistence, and resident remove/replaceIfPresent patches while preserving Reactor's authoritative-server-then-local-overlay behavior. Required contracts include confirmation-proven active rows, server/query retraction footprints, watermark-pruned and failed-active roots, global/delete/ref/lookup/deferred effects, components over both page limits without full residency, 10,000 disjoint exact work/decode metrics, empty-op watermark, and closure/store/row races. Preserve every terminal/deferred/retry/window edit; tests first; no SwiftPM until parser/diff freeze and explicit root release. No commit.
- 2026-08-10T17:06:19-0400 infinite-query-retention-window-source-freeze: all six owned files are source-frozen. Public vocabulary adds accumulated-by-default retention, `.window(maximumPageCount:)`, previous-page capability/action, and wrapper/client forwarding. The local coordinator keeps a contiguous bounded window through 100 forward loads, backward navigation, prepend, and reorder while pruning hydrated values to visible identities. The live coordinator bounds retained data chunks, cancels and removes evicted subscriptions/chunks, keeps one payload-bounded leading watcher, and supports forward/backward/forward cursor reconstruction. Focused parity, deferred-residency, and typed-API regressions cover page identifiers/flags, active subscription bounds, cancellation, decoded-value/cache pruning, validation, and public forwarding. All six files pass `swiftc -frontend -parse`; tracked plus untracked scoped whitespace checks pass; non-strict `swift-format lint` exits 0 with pre-existing shared-file warnings. No SwiftPM command has run. Scribe files are released and remain untouched. Awaiting root's serialized SwiftPM slot.
- 2026-08-10T17:07:17-0400 infinite-query-retention-window-first-compile-fix: the serialized retry gate stopped before tests because Swift could not infer the two `compactMap` result types in `retainedPageChunkKeys`. Only those local arrays now declare `[InstantLiveInfiniteRetainedChunkKey]`, which also makes reverse-plus-forward addition unambiguous. The owned source passes `swiftc -frontend -parse` and its scoped whitespace check; it is refrozen. No SwiftPM command was run by this worker.
- 2026-08-10T17:18:00-0400 infinite-query-retention-window-first-test-fix-freeze: the 202-row local stress seed now uses three transactions of at most 100 entities (200 steps), staying below the existing 256-step delivery limit. The live failure was a canceled-before-start race: an evicted task removed from the coordinator dictionary could later begin and register an orphan query. Starter/chunk tasks now check cancellation before and immediately after async registration; the regression compares exact active query sets after forward/previous/forward, proves any registered frozen query is removed, and requires registered/removed sets to match after unsubscribe. The typed wrapper fixture now gives its direct subscription and wrapper task separate single-consumer streams. All three edited files pass `swiftc -frontend -parse`; the six-file scoped tracked/untracked whitespace checks pass. Source is refrozen and no SwiftPM command was run by this worker.
- 2026-08-10T18:15:53-0400 bounded-server-apply-rebase-verified-and-released: root's exact frozen `swift test --jobs 1 --no-parallel --filter InstantBoundedServerApplyRebaseTests` run exited 0: build 9.97 seconds; 12/12 tests passed in 2.018 seconds. The 10,000-row disjoint contract passed in 1.303 seconds with zero body decodes and zero queue-wide reads; 101 connected rows passed in 0.090 seconds with 50/50/1 reverse and forward pages; the component over 8 MiB passed in 0.475 seconds; store, row/status, and closure race retries passed in 0.021/0.017/0.017 seconds. Final read-only review confirms full mutation-body residency is capped at 50 rows and 8 MiB of actual SQLite blob bytes per reverse/forward page, the final store/outbox/query/metadata write is one `BEGIN IMMEDIATE` transaction guarded by exact store/outbox/row/closure revisions, and success/stale/precommit-error paths delete the connection-local TEMP plan (cleanup failures leave only unique-ID inert TEMP rows until connection close). Automatic apply/live-refresh/receive and confirm-if-present are addressed; the only queue-wide helper callers are explicit `confirmMutation`, `failMutation`, `discardFailedMutation`, `retryMutation`/`retryFailedMutation`, and `drainPendingMutationsLocally`. Resident patches use only `remove`/`replaceIfPresent` in pages of 50 compact lifecycle rows. Two boundedness follow-ups are explicit: compact resident patch pages have no byte cap, and test telemetry retains cumulative per-page count arrays; neither affects the proven 50/8-MiB full-body window, but neither should be described as strict process-lifetime constant memory. Full package tests were not run. Source remains frozen and uncommitted; this worker releases Runtime/SQLite/server-apply-test/bounded-fixture ownership and the serialized SwiftPM slot.
- 2026-08-10T18:18:01-0400 bounded-server-apply-final-bounds-follow-up: root re-delegated the two final audit gaps to `retry_window_implementation`, which reacquires only `SQLitePersistenceStore.swift` server-apply metrics/resident-patch helpers and `InstantBoundedServerApplyRebaseTests.swift`. Test-first scope: cap every compact lifecycle patch page at 50 rows and 8 MiB of actual SQLite blob bytes with loud fail-closed handling before loading an oversized row, replace cumulative page-count arrays with constant-size counters/maxima, prove near-limit failure-message paging, and stress 10,000 metric page records without retained collections. Preserve all concurrent source edits; parser/diff freeze before requesting SwiftPM. No commit.
- 2026-08-10T18:22:47-0400 bounded-server-apply-final-bounds-source-freeze: source/tests are frozen. Resident patch planning now selects only identity/order/delete state plus `length(CAST(staged_lifecycle_json AS BLOB))`, admits at most 50 rows and 8 MiB, then loads each admitted compact lifecycle by addressed key; a missing, changed, or individually oversized lifecycle fails loudly before unbounded actor allocation. Staging separately rejects any compact lifecycle over 8 MiB before durable commit. The previous cumulative reverse/forward page arrays are gone; fixed counters retain directional page/row totals, maxima, and last-page sizes, plus resident patch page/row/maxima. Focused tests replace 50/50/1 array history with sufficient fixed-counter proofs, record 10,000 synthetic body pages and assert no retained integer array field, and use an 8,000,000-byte failure message plus a 600,000-byte successor to prove resident lifecycle paging splits at the actual SQLite byte ceiling while queue-wide reads remain zero. Both files pass `swiftc -frontend -parse`; scoped tracked diff whitespace and untracked test whitespace checks pass; stale page-history identifiers are absent. No SwiftPM command ran. Awaiting root's explicit serialized-lane release.
- 2026-08-10T18:26:21-0400 bounded-server-apply-final-bounds-verified-and-released: root's exact frozen `swift test --jobs 1 --no-parallel --filter InstantBoundedServerApplyRebaseTests` run exited 0. Build completed in 164.13 seconds; all 14 focused tests passed in 2.727 seconds. The new 10,000-page constant-metrics regression passed in 0.001 seconds and the near-limit compact lifecycle resident-patch byte-ceiling regression passed in 0.662 seconds. The complete server-apply slice is source-frozen and uncommitted; the full package suite was not run. This worker releases `SQLitePersistenceStore.swift`, `InstantBoundedServerApplyRebaseTests.swift`, and the serialized SwiftPM slot to root with no remaining boundedness defect identified.
- 2026-08-10T19:47:38-0400 streaming-storage-upload: root delegated the storage upload memory fundamental to `streaming_storage_upload` under the existing umbrella plan. This worker owns `InstantStorageTransport.swift`, the `uploadFile`/storage-request-only section of `InstantRuntime.swift`, and focused `InstantStorageHTTPParityTests.swift` / `InstantStorageRuntimeTests.swift` additions. The Runtime overlap is explicit: preserve every deferred, terminal, retry, server-apply, and other concurrent edit outside the narrow storage section. Test first; no branch switch, stash, reset, commit, or SwiftPM until root releases the serialized lane.
- 2026-08-10T20:01:00-0400 streaming-storage-upload-source-freeze: source and focused tests are frozen. `InstantStorageUploadRequest` now carries `sourceURL` plus exact `byteCount`; Runtime's only owned hunk measures the regular file and no longer calls `Data(contentsOf:)`; the live HTTP adapter sets `Content-Length`, keeps `URLRequest.httpBody` nil, and uses `URLSession.upload(for:fromFile:)` with cancellation normalization. The 64-MiB sparse-file regression records only the file URL, exact length, and zero retained request-body bytes; existing PUT headers, 403/decode failures, single-attempt behavior, local caching/delete cleanup, and in-flight cancellation remain covered. Strict Swift 6 standalone typecheck of the complete transport with an `InstantError` stub passed; all four owned source/test files pass frontend parse and scoped diff whitespace checks. Scribe has zero direct migration sites; Instant constructors are only Runtime plus the two focused HTTP test helpers, all migrated. SwiftPM has not run. Exact next step: root may run `swift test --jobs 1 --no-parallel --filter InstantStorageHTTPParityTests` and `swift test --jobs 1 --no-parallel --filter InstantStorageRuntimeTests` in the serialized lane, then return type/test failures to this worker if needed.
- 2026-08-10T21:13:49-0400 persistence-revision-domains: `instant_revision_domains` owns only cache/revision-domain sections of `SQLitePersistenceStore.swift` and runtime persistence-state adoption / `replaceSnapshot` sections of `InstantRuntime.swift`, plus new focused `InstantPersistenceCacheResidencyTests.swift`. Scope is independent store, attribute, outbox, and query-result revision validity: outbox-only, query-result-only, automatic-retry, and metadata-only server-refresh changes must preserve the materialized store and avoid full snapshot load/reconstruction/replacement, while a real second-runtime store mutation forces one safe reload. Preserve every bounded body/page, deferred hydration, terminal, retry-pump, server-apply, infinite-query, typed-selection, and streaming-upload edit. Tests first. Source/test edits are frozen until root explicitly releases the current Scribe SwiftPM lane; parser/diff checks precede any serialized SwiftPM request.
- 2026-08-10T21:24:52-0400 bounded-query-materialization: `instant_revision_domains/bounded_query_materialization` owns query-enumeration-only sections of `TripleIndexes.swift`, query-materialization-only sections of `InstantStore.swift`, and new focused `InstantBoundedQueryMaterializationTests.swift`. Explicit overlap split: `instant_deferred_values` retains all deferred residency, removal, and hydration methods and every existing Store/index hunk; `instant_revision_domains` retains persistence cache/revision adoption and `InstantPersistenceCacheResidencyTests.swift`; this lane may change only candidate enumeration, bounded ordering/selection, and deterministic query-work counters. Preserve filtering, ordering, offset/cursor, joins/reverse lookup, deferred-value behavior, emissions, and unbounded-query semantics. Test first; no `InstantRuntime.swift`, `SQLitePersistenceStore.swift`, SwiftPM, or commit. Parser and scoped diff freeze before handoff.
- 2026-08-10T21:43:26-0400 persistence-revision-domains-source-freeze: revision/cache source and the focused residency suite are frozen. SQLite now tracks store triples, attributes, outbox lifecycle, and persisted live-query results independently; its thin cache carries materialized-store validity separately from scalar/outbox/query validity and returns an explicit none/attributes/full-snapshot adoption decision. Runtime adopts only that decision, removes redundant full snapshot replacements from query and attribute retry paths, and exact server-apply plans compare all four revision domains. Successful in-process server apply advances the cache's exact domain revisions and targeted resident state, while an external store revision mismatch remains the only compact-load path that reconstructs and replaces the full store. The 10,000-triple tests cover 2,000 alternating outbox/query-only revisions, 101 automatic retries in 50/50/1 windows, 1,000 metadata-only server applies, an in-process real server refresh, an external attribute-only refresh, and a second-runtime real triple mutation requiring exactly one load/reconstruction/replacement across repeated reads. Focused `swiftc -frontend -parse`, tracked/untracked whitespace checks, and test-file `swift-format lint` pass. SwiftPM has not run; exact requested command is `swift test --jobs 1 --no-parallel --filter InstantPersistenceCacheResidencyTests` after serialized-slot release.
- 2026-08-10T22:04:48-0400 revision-and-bounded-query-refreeze: the first serialized residency run compiled all production source and stopped only on actor awaits inside synchronous test comparison autoclosures. Every awaited/throwing test value is now prebound, including the retry optional before `#require`; focused parser, source/test whitespace, and test formatting checks pass. Bounded query also completed its parity refreeze: finite implicit-order indexed equality uses bounded `serverCreatedAt` selection with one lookahead and exact cursors/page info; zero finite bounds and every compound limit/first/last pair are rejected in static, direct, and nested materialization; observation waits have cancellation-safe five-second fences. Its three owned files pass frontend parse and scoped tracked/untracked whitespace checks. SwiftPM remains idle. Exact commands remain `swift test --jobs 1 --no-parallel --filter InstantPersistenceCacheResidencyTests` and `swift test --jobs 1 --no-parallel --filter InstantBoundedQueryMaterializationTests`.
- 2026-08-10T21:53:53-0400 bounded-query-materialization-source-freeze: query source and the focused bounded-materialization suite are frozen. Finite top-level queries retain only offset plus requested rows plus one page-information lookahead in a bounded heap; finite reverse/forward includes retain only their requested child window and materialize selected snapshots after selection. Indexed equality candidate sets are intersected by membership without a full sorted intermediate, direct reverse-index keys avoid an extra full child-ID array/set, and deterministic counters expose candidates examined/matched, snapshots materialized, maximum candidates retained, and bounded selections. Stress coverage exercises 10,000-row root ordering with limit/offset/cursors/last, indexed filtering against unbounded results, a 10,000-child reverse include with a deferred payload, and reordered observation emissions. Existing Swift indexed-equality/no-explicit-order entity-ID behavior is deliberately preserved; its upstream implicit-order difference remains parity debt. Scribe list queries already provide an explicit root limit and bounded transcription/segment/attachment includes, so this is an implementation-only improvement with no list-retention or deferred-residency API change. The three owned files pass `swiftc -frontend -parse` and scoped diff whitespace checks. No SwiftPM command ran; exact requested command is `swift test --jobs 1 --no-parallel --filter InstantBoundedQueryMaterializationTests`.
- 2026-08-10T21:59:09-0400 bounded-query-observation-timeout-refreeze: after the serialized lane stopped for an unrelated revision-suite test-compilation failure, root released a test-only hygiene follow-up. Both bounded-observation emission waits now race one stream read against an explicit five-second sleep in a throwing task group; success cancels the sleeper, timeout cancels the stream read, and either child exits cancellation-aware rather than pinning the serialized suite. Production query source is unchanged from the prior freeze. The three owned source/test files pass `swiftc -frontend -parse` and the scoped diff whitespace check; no SwiftPM command ran. Exact requested command remains `swift test --jobs 1 --no-parallel --filter InstantBoundedQueryMaterializationTests`.
- 2026-08-10T22:04:26-0400 bounded-query-upstream-parity-refreeze: root promoted the two upstream-review debts into this slice. The finite indexed-equality/no-explicit-order shortcut is removed: those queries now use the bounded `serverCreatedAt` selector, retain exactly requested rows plus one page-information lookahead, and return creation-time cursors plus exact next/previous flags. Static validation and direct materialization now require every finite bound to be positive and permit at most one of `limit`, `first`, or `last`; nested include validation reports its relation pagination path and invalid nested materialization fails closed. New focused contracts use deliberately anti-correlated entity IDs/creation times to prove implicit order, exact cursors/pageInfo, two materialized snapshots, and three retained candidates, then cover zero for each bound and every pairwise compound bound. The cancellation-safe five-second observation fences remain. All changes stay inside the existing TripleIndexes query/validation, InstantStore query-metrics, and focused-test claims; no validation file overlap arose. The three owned files pass `swiftc -frontend -parse`; tracked scoped whitespace passes, the untracked test's `git diff --no-index --check` emits no whitespace diagnostics (status 1 only because the file is new), and stale shortcut/old-validation text is absent. No SwiftPM command ran. Exact requested command remains `swift test --jobs 1 --no-parallel --filter InstantBoundedQueryMaterializationTests`.
- 2026-08-10T22:17:01-0400 terminal-encoding-retry-classification-follow-up: the full serialized package run exposed a hot loop in `fiftyEncodingFailuresUseBoundedRowAddressedQuarantineAndDoNotStarveTail`: one bounded set of 50 schema/encoding failures is quarantined, the generic automatic failed-row selector immediately classifies those terminal failures as retryable, and the same pump re-encodes/re-quarantines them indefinitely at approximately 98% CPU. Root assigned `instant_revision_domains` a narrow test-first follow-up in the already shared Runtime/SQLite claims plus the existing focused bounded-outbox regression. Ownership is limited to terminal-vs-transient retry classification in `retryAutomaticFailedMutationWindow`/eligibility SQL, automatic pump termination and deterministic testing hooks only as required, and the named focused test. Prior retry, terminal-component, delivery, server-apply, revision-domain, and query work stays preserved. The previous retry/server-apply owners have explicitly released these sections; root owns the only remaining test-file claim. No sleep/backoff, queue-wide read, unrelated production edit, SwiftPM run, or commit is authorized before parser/diff source freeze and serialized-slot release.
- 2026-08-10T22:36:36-0400 query-priority-wait-race-follow-up: root authorized `instant_revision_domains` to claim only `queryThatInitiatesReconnectPrecedesPersistedMutations` in `InstantOutboxDeliveryStallTests.swift`. The pending mutation never enters failed-row revision eligibility; the new scalar attribute-revision read exposes the test's existing asynchronous pump race after query completion. Scope is one cancellation-safe five-second wait for the transact before the existing add-query/transact order assertion. The worker also retains the existing bounded-outbox fixture claim solely to keep the already-migrated 0015 effect-entity marker while replaying outbox migrations 0012/13/14/16/17/18. No other stall, migration, delivery, or Runtime behavior changes.
- 2026-08-10T23:23:44-0400 revision-domain-query-retry-verified-and-released: `instant_revision_domains` releases all cache/revision, query-materialization, terminal retry-classification, bounded-outbox fixture, and stall-test claims to root. Focused gates are green: cache residency 3/3; bounded query 6/6; one-pass 50-row encoding quarantine plus deployed-schema recovery 2/2; every returned asynchronous-connect regression; and the final `deepBacklogIsFlushedInBoundedBatches|weightedBacklogRefillsAfterAcknowledgements` run 2/2 in 0.443 seconds. Schema-resolution failures are retryable only after the durable attribute revision changes, while timeout/service failures remain independently eligible. Test fixtures now fence asynchronous delivery for five seconds and mutate legacy SQLite state only before connection. The autoresearch ledger validates at 22 records with only the scenario-contracted physical-footprint slope on both owned hypotheses. Source is frozen and uncommitted; no physical measurement, KEEP decision, or completed full-package gate is claimed. Root owns final review, remaining integration verification, commit/ledger bookkeeping, and physical acceptance.
- 2026-08-10T23:44:00-0400 full-suite-integration-audit: root's first complete `swift test --jobs 1 --no-parallel` attempt built successfully but exited 1 after exposing cross-slice integration failures and a SIGBUS. Root claims the affected discard/live/outbox/query/reconnect/validation test paths for exact focused repair. Current evidence separates asynchronous-connect assertion races from real regressions: finite-query validation incorrectly rejects upstream-valid compound top-level pagination; synthetic `id` equality can miss entities without an explicit primary-key triple; mutation deadline wakes reuse reconnect backoff sleep and busy-loop when that test double returns immediately; live infinite-query window eviction and transient server-500 retry need isolated reruns. No release or physical claim is made; clean rebuild plus focused red/green evidence precede the next full suite.
- 2026-08-10T23:57:16-0400 infinite-window-late-registration-repair: root delegated `instant_window_leak_fix` the bounded-window sections of `InstantInfiniteQuery.swift` and `InstantInfiniteQueryParityTests.swift`. Diagnosis: the post-registration cancellation guard can abandon a newly registered live-query stream before an iterator owns its termination, allowing a frozen between-cursor query to survive eviction. This worker will add a deterministic cancellation-during-registration contract, make stream teardown cancellation-safe, freeze parser/diff evidence, and request the serialized SwiftPM lane before testing. Prior deferred and route-window edits remain preserved; no Runtime/SQLite/other-file edits.
- 2026-08-11T01:08:27-0400 durable-benchmark-seed-migration: root delegated `instant_benchmark_seed_limits` the exact full-suite fixtures and local-todos benchmark source that still submit more than 256 transport steps as one durable mutation. This worker owns `InstantDiagnosticFeedbackLoopTests.swift`, `BenchmarkTests.swift`, `LinkedInfiniteScribeShapedMemorySoakTests.swift`, `ScribeShapedAuthenticatedIdleMemorySoakTests.swift`, and `InstantLocalTodoBenchmark.swift`; the older `codex-019fe75c` BenchmarkTests claim is preserved and no longer live. Scope is deterministic bounded transactions with entity precondition groups kept atomic, unchanged operation/dataset cardinalities, updated deterministic metric expectations, parser/diff freeze, and no SwiftPM before root releases the serialized lane.
- 2026-08-11T00:05:55-0400 infinite-window-late-registration-verified: the post-registration early returns are removed for starter and page-chunk tasks. Once runtime registration returns a stream, even an already-cancelled task enters iteration so AsyncStream cancellation runs the live-query unregister handler; cancellation before registration still exits at the pre-registration guard. Root's clean serialized run of `liveWindowEvictsAndCancelsOppositeCursorChunkInBothDirections` passed 1/1 in 0.051 seconds, including forward eviction, previous-page eviction, forward-again replacement, and complete unsubscribe cleanup. Both owned files remain source-frozen for the full package suite; no commit was made.
- 2026-08-11T01:07:59-0400 full-suite-parity-fixture-cleanup: root delegated `public_confirm_row_addressed` the stale parity and inventory failures from `/tmp/instant-full-suite-20260811-e.log`. This mower owns only `InstantReactorParityTests.swift`, `InstantSQLiteDataParityReconciliationTests.swift`, `InstantStartupTraceTests.swift`, `InstantStoreParityTests.swift`, `RecipesContractTests.swift`, and the SQLiteData upstream inventory/pin documentation. It preserves all production source and concurrent hunks, may coordinate a source defect back to root rather than edit it, and will use parser/diff checks only until root releases the serialized SwiftPM lane.
- 2026-08-11T01:10:42-0400 parity-scope-expansion: root authorized `public_confirm_row_addressed` to remove only the indexed-equality/no-explicit-order shortcut in `TripleIndexes.swift`, retaining the exact upstream reverse-cascade ordering regression, and to add the 52 newly vendored SQLiteData tests as honest blocked/not-applicable records in `InstantParityCoverage.swift`. Every bounded-query, deferred-residency, and other inventory hunk remains preserved; parser/diff checks only until root releases SwiftPM.
- 2026-08-11T01:18:09-0400 full-suite-parity-source-freeze: `public_confirm_row_addressed` froze the delegated slice after correcting the log-hidden 53rd SQLiteData record. Reconciliation now counts normalized source-file/test-name pairs, including duplicate test names; the 1.9.0 inventory is exact at 48 files and 314 tests, with 49 sectioned-fetch blockers and four CloudKit-only exclusions. The equality-filter shortcut no longer bypasses implicit `serverCreatedAt` order, Reactor waits are cancellation-bounded, compact startup no longer claims outbox-body decoding, and Recipes expectations match the aggregate schema. All seven changed Swift files pass frontend parsing and the scoped diff passes whitespace checks; no SwiftPM command has run. Root owns the serialized focused command and cross-suite count fixture updates.
2026-08-11T01:08:00-0400 root claims InstantStoreTests stale revision/cache/query-validation expectations and the three Swift concurrency safety comments under plan 2026-08-09-bounded-intent-outbox-memory. Full-suite evidence is /tmp/instant-full-suite-20260811-e.log; no production semantics will be weakened to satisfy stale tests.
- 2026-08-11T01:11:46-0400 pump-reconnect-audit split: root delegated the exact `observeConnectionStatusPublishesRuntimeStatusChanges` hunk in `InstantStoreTests.swift`, `runtimeLiveMutationEncodingFailureDoesNotBlockTheFollowingMutation` in `InstantLiveTransportTests.swift`, and the late signal-11 regression in `InstantMessageServerAcceptanceTests.swift`. This worker preserves every production Runtime/SQLite edit and every unrelated test hunk. The status fixture will retain ordered-failure semantics, the issue assertion will wait on durable quarantine inside `withKnownIssue`, and the crash repair will use revision-only persistence evidence after two crash reports placed invalid retains in the oversized test's post-`loadState().snapshot` async resume. Parser/diff checks only until root releases SwiftPM.
- 2026-08-11T01:14:30-0400 pump-reconnect-audit source freeze: all three delegated test hunks now match the production boundaries. The connection-status fixture places its failed row after the pending row, so explicit flush may legally confirm the ordered head while preserving the newer failure metadata. The encoding test waits for durable failed quarantine before leaving `withKnownIssue`. The public-resolution race retains only four inline revision scalars across the two expected throwing calls; both crash reports and latest test-binary disassembly place the fault in `swift_retain`/`objc_retain` of the older full snapshot after the second actor load returns. All three files pass `swiftc -frontend -parse` and scoped `git diff --check`; no SwiftPM command ran. CLI local-flush failure is separately classified as a stale fixture because the focused bounded-outbox contract deliberately stops behind an unproven local-only confirmed head; benchmark owner and root were notified.
- 2026-08-11T01:38:17-0400 attribute-revision-cas: `instant_diff_compile_audit` owns only expected-attribute-revision compare-and-swap plumbing in `SQLitePersistenceStore.swift`, its exact `InstantRuntime.swift` callers, focused stale-revision additions in `InstantPersistenceCacheResidencyTests.swift`, and mechanical signature migrations in `InstantTerminalFailureComponentTests.swift` / `InstantOutboxHydrationTests.swift`. It preserves the current exact returned-revision metadata-snapshot adoption hunk and every cache/deferred/query/terminal-component implementation hunk. Tests first; parser/diff checks only; no SwiftPM or commit.
- 2026-08-11T01:42:55-0400 attribute-revision-cas-source-freeze: the slice is frozen. Every revision-checked full-snapshot overload, `saveLocalMutation`, and claimed-terminal load/commit now requires and atomically compares `expectedAttributeRevision`; the local-mutation cache read/adoption predicates compare the same domain. Runtime passes the compact state attribute revision at ordinary enqueue, migration, legacy terminal fallback, discard, retry, terminal preparation, and terminal commit. Existing terminal and metadata-snapshot test callers are migrated. Three focused regressions prove S0/A0/O0 -> S0/A1/O0 rejects stale local mutation and full snapshot without erasing the new attribute, while S0/A0/O1 -> S0/A1/O1 rejects terminal preparation before body decode and rejects the prepared commit. The metadata-key `saveSnapshot` overload retains its pre-existing exact returned store/attribute/outbox revisions and cache adoption. All five changed Swift files pass one frontend parse invocation; tracked scoped `git diff --check` passes; the untracked residency test emits no whitespace diagnostics under `git diff --no-index --check` (status 1 only because it is new). SwiftPM did not run. Root owns serialized compilation/testing.
- 2026-08-11T01:52:59-0400 remaining-attribute-revision-domains: after root's serialized gate exited, `instant_diff_compile_audit` resumes the existing SQLite/cache/hydration claims and adds caller-only claims for `InstantLiveTransportTests.swift`, `InstantServerAcceptanceProofTests.swift`, and `InstantStoreTests.swift`. Scope is mandatory revision-checked store-snapshot attribute CAS, exact metadata-store/live-refresh store-vs-attribute revision bump/adoption, all direct test callers, stale attribute-race coverage, and deliberately divergent metadata-snapshot revision coverage. Preserve every unrelated hunk; tests first; parser/diff freeze only; no SwiftPM or commit.
- 2026-08-11T01:38:09-0400 selective-deferred-strip split: root delegated `deferred_strip_bounded` only the deferred stripping/preparation metadata sections of `TripleIndexes.swift` and `InstantStore.swift` plus focused `DeferredValueResidencyTests.swift` additions. Scope is a reusable exact entity/attribute-key removal path, consumption of the prepared indexes so commit does not trigger another whole-index copy, and a 10,000-row deterministic work-counter contract proving one affected deferred key does not scan the store. Preserve every bounded-query, runtime, SQLite, pagination, and unrelated deferred hunk; parser/diff checks only and no SwiftPM.
- 2026-08-11T01:44:02-0400 selective-deferred-strip source freeze: the delegated slice is frozen. `TripleIndexes` carries an exact transient set of hydrated/written entity+attribute keys through every prepared-index copy and Runtime wrapper, then drains it with direct `eav[entity][attribute]` lookups; no entity/index enumeration or store-sized removal buffer remains. Snapshot installation filters deferred attributes while building the index instead of building then stripping it. `InstantStore.commit` consumes the prepared mutation, strips its uniquely transferred indexes in place, installs those indexes, and returns the same prepared value with fixed-size counters. `commitStripsOnlyTheAffectedDeferredKeyFromTenThousandRows` seeds 10,000 resident title rows plus 10,000 deferred payload rows, hydrates/updates one payload, and requires pending-key high-water 1, examined keys 1, resident keys 1, removed values 1, final pending keys 0, and hot triple count 10,000. Actual Swift 6.3 frontend parsing and scoped tracked/untracked whitespace checks pass; a standalone Swift 6.3 ownership typecheck confirms the `consuming` parameter form. The repository's Swift 5.8-era `swift-format` cannot parse ownership modifiers and reports its existing shared-file warnings, so it is not type evidence. No SwiftPM command ran. Exact requested gate: `swift test --jobs 1 --no-parallel --filter commitStripsOnlyTheAffectedDeferredKeyFromTenThousandRows`.
- 2026-08-11T01:59:20-0400 remaining-attribute-revision-domains-source-freeze: the follow-up is source-frozen. Both revision-checked `saveStoreSnapshot` overloads and `saveLiveRefresh` now require `expectedAttributeRevision` and atomically compare store/outbox/attribute revisions. The metadata-key store path and live-refresh path compute triple and attribute changes separately, bump only the changed revision domains, return the exact resulting revisions from the transaction, and adopt cache state only from matching store/outbox/attribute bases; query-only live refresh therefore also rejects an attribute-stale actor. Every direct Runtime/test caller is migrated. Focused tests cover metadata-store and query-only live-refresh S0/A0/O0 to S0/A1/O0 rejection, successful attribute-only metadata-store and live-refresh S0/A0/O0 to S0/A1/O0 bumps, and exact memory-cache adoption from deliberately divergent S3/A5/O7 to S4/A6/O8. All six affected Swift files pass one `swiftc -frontend -parse` invocation; scoped tracked whitespace checks pass, and the untracked residency test emits no whitespace diagnostics under `git diff --no-index --check` (status 1 only because the file is new). SwiftPM did not run. Root owns serialized compilation/testing.
- 2026-08-11T03:59:26-0400 validation-immediate-abort-migration: `validation_abort_migration` owns only `InstantLiveSessionValidation.swift`, the three InstantSwiftDataTesting live-validation transport decorators, and focused existing validation/decorator test files. Scope is deterministic test-first enforcement of the root-owned `InstantLiveWebSocketSession` immediate-abort capability: reject incapable sessions before wire I/O; use an exact five-second bound with synchronous abort for every validation send/receive; gracefully close only through the shared bounded close helper on success and synchronously abort on failure; forward the base abort through all three decorators. Preserve all transport/runtime/live-transport tests and every unrelated dirty hunk. Parser/diff source freeze only; no SwiftPM or commit.
- 2026-08-11T04:09:46-0400 validation-immediate-abort-source-freeze: the abort migration is source-frozen. Core live-session validation rejects a legacy session before its first wire operation, defaults every send/receive and graceful close to exactly 5,000ms, allows only a named 1...5,000ms deterministic test override, drives every deadline through one helper whose abandonment synchronously aborts the exact session, bounds successful graceful close through `closeGracefully`, and uses only synchronous idempotent abort on every failure path. VoiceTrail, Playback, and Sharing decorators now call `session.forwarding`, preserving the base identity, abort handle, and incapable legacy state. Focused causal tests cover pre-wire capability rejection, a stuck send, a stuck graceful close, capable identity/abort forwarding through all three decorators, and legacy capability preservation. All seven owned Swift files pass one frontend parse invocation and scoped whitespace checks; no SwiftPM command ran. Root separately authorized a follow-up in these same files to replace unrelated existing 10/20-second validation waits with exact five-second bounds.
- 2026-08-11T04:14:49-0400 validation-five-second-law-source-freeze: the separately authorized timeout follow-up is source-frozen. One package timing contract fixes the production budget at 5,000ms and cooperative poll interval at 25ms / 200 attempts. Both VoiceTrail role/revocation waits and all five Sharing value/outbox/share waits use that exact bound; Sharing's timeout evidence now reports five seconds. Playback deletes its cancellation-waiting 20-second task-group implementation and delegates to the single proven hard `instantLiveWithTimeout` helper at the shared 5,000ms budget, while its reconnect poll uses the same interval. Focused tests pin the shared numeric contract and statically require all three production decorators to use capability-preserving forwarding and the three validation sources to contain no former 800/400-attempt or 20/10-second forms. The abort tests remain `liveSessionValidationRejectsLegacySessionBeforeWireIO` and `liveSessionValidationTimeoutAbortsStuckSendExactlyOnce`; five-second tests are `sharedLiveValidationTimingIsExactlyFiveSeconds` and `liveValidationSourcesPreserveAbortAndFiveSecondContracts`. All six changed owned Swift files pass one frontend parse invocation and scoped whitespace checks; no SwiftPM command ran. Root owns the ready serialized SwiftPM gate.
- 2026-08-11T04:21:19-0400 validation-abort-and-five-second-verified-release: root's clean final frozen gate compiled and passed all four owned tests (`liveSessionValidationRejectsLegacySessionBeforeWireIO`, `liveSessionValidationTimeoutAbortsStuckSendExactlyOnce`, `sharedLiveValidationTimingIsExactlyFiveSeconds`, `liveValidationSourcesPreserveAbortAndFiveSecondContracts`) as part of a 15-test lifecycle gate: 15/15 passed in 0.166 seconds after a 153.08-second build. Parser and scoped whitespace evidence remain green. `validation_abort_migration` releases all six changed source/test claims to root for integration, review, commit, ledger, and broader/physical verification. No commit was made by this worker.
- 2026-08-11T04:28:38-0400 connection-attempt-lifecycle: root extends the existing mower plan after a read-only audit proved runtime and validation still await arbitrary custom connector work before owning an abort handle. Root owns the new cold per-call `InstantLiveConnectionAttempt` abstraction plus the narrow Runtime/validation integration and focused transport tests. The attempt must expose synchronous idempotent abort before async work, abort late-returning sessions, preserve identity/capability through decorators, and keep overlapping attempts independent. Existing fixture/decorator owners will receive mechanical migration splits only after the core API freezes. No SwiftPM runs until parser/diff freeze.
- 2026-08-11T04:34:01-0400 connection-attempt-fixture-split: `attempt_api_fixture_migration` owns only `BootstrapTests.swift`, `InstantMessageServerAcceptanceTests.swift`, and `TypedAPITests.swift`. Scope is the mechanical migration of successful custom transports to the cold abortable connection-attempt API, including capability-preserving mapping of `.local`; prior fixture abort behavior and every unrelated dirty hunk are preserved. Parser/scoped diff only; no SwiftPM or commit.
- 2026-08-11T04:35:29-0400 connection-attempt-fixture-source-freeze: the three assigned test files are frozen. Bootstrap now creates one session and cold attempt per request, records the request inside the async connector, and wires the attempt's synchronous abort to that exact already-created session. Acceptance uses `.immediate` for its synchronously materialized capable session. Typed API wraps `.local` with `mapSessions`, retaining the base attempt/session identity and abort capability instead of calling the legacy `connect` closure. One frontend parse for all three files, scoped whitespace checks, and a search excluding old custom constructors/direct `.local.connect` calls pass. No SwiftPM command ran. Remaining risk is package-level typechecking/runtime integration of the new API, owned by root's serialized gate.
- 2026-08-11T04:33:47-0400 attempt-decorator-migration: `attempt_decorator_migration` owns only the VoiceTrail, Playback, and Sharing live-validation transport decorators plus the VoiceTrail source-contract test. It will replace direct `base.connect(request)` decoration with the root-owned capability-preserving `base.mapSessions` combinator, require that shape statically, preserve every unrelated abort/five-second hunk, run parser/scoped whitespace checks only, and freeze without SwiftPM or commit.
- 2026-08-11T04:34:40-0400 attempt-decorator-migration-source-freeze: all three production decorators now use `mapSessions`, so the root-owned connection-attempt identity, pre-work abort capability, and late-session cleanup survive validation tracing/reconnect decoration. The static source contract requires `mapSessions` plus `session.forwarding` and rejects the deprecated wrapper constructor and direct `live.connect(request)` / `base.connect(request)` forms. All four owned Swift files pass one frontend parse invocation and scoped whitespace checks; no SwiftPM command ran. Source is frozen for root integration and verification; no commit was made.
- 2026-08-11T04:03:14-0400 abort-test-fixture-migration: root delegated only successful scripted `InstantLiveWebSocketSession` fixture migrations in `InstantReactorParityLiveSupport.swift`, `InstantOutboxDeliveryStallTests.swift`, `InstantOutboxHydrationTests.swift`, `InstantFailedMutationDiscardTests.swift`, `InstantStoreTests.swift`, `InstantMessageServerAcceptanceTests.swift`, and `BootstrapTests.swift`. Existing owners retain every unrelated hunk. This lane will supply prompt, thread-safe, idempotent abort behavior that releases pending send, receive, and close work; it will not use an async wrapper or no-op abort, touch production/validation/transport tests, run SwiftPM, or commit. Frontend parse and scoped diff only.
- 2026-08-11T04:33:57-0400 connection-attempt-core-fixtures: `attempt_core_fixture_migration` owns only constructor migration in `InstantReactorParityLiveSupport.swift`, `InstantFailedMutationDiscardTests.swift`, `InstantOutboxDeliveryStallTests.swift`, `InstantOutboxHydrationTests.swift`, `InstantStoreTests.swift`, and `ValidationHarnessTests.swift`. It will use the cold immediate factory for synchronously available sessions and exact lock-backed pending-connector aborts for asynchronous attempts, preserving all prior scripted session-abort work. Parser/scoped diff only; no SwiftPM or commit.
- 2026-08-11T04:40:49-0400 connection-attempt-core-fixtures-source-freeze: all 12 deprecated constructor call sites in the six delegated files are migrated. Synchronously materialized sessions use `.immediate`; synchronous failures throw from `.connectionAttempts`; actor-isolated and deliberately blocked connectors use `InstantLiveConnectionAttempt(connect:abort:)` with one shared lock-backed continuation owner. Its synchronous abort clears and resumes the exact pending connect, cancels any connector task, and aborts a buffered or late session. The two release-gate actors now retain only the abortable owner rather than an uncancellable checked continuation. One Swift frontend parse invocation and scoped whitespace checks pass; no SwiftPM command or commit ran. Root owns typechecking and behavioral gates.
- 2026-08-11T05:00:55-0400 infinite-query-terminal-cleanup: `infinite_query_terminal_cleanup` owns only terminal cleanup/retention paths in `InstantInfiniteQuery.swift` and terminal-retention/frozen-query-eviction tests in `InstantInfiniteQueryParityTests.swift`. Scope is centralized release of live/local chunk graphs, cursor/navigation sets, prebootstrap/hydration caches, and full latest emissions after cancellation/failure while preserving terminal emission ordering; plus a 10,000-row bounded-retention regression and non-vacuous exact frozen-query add/remove assertions. `infinite_query_setup_cancellation` separately owns auth/startup/pre-lease/setup/replacement cancellation and will not be overwritten. Parser/scoped diff only; no SwiftPM or commit.
- 2026-08-11T05:00:47-0400 connection-attempt-postfix: `connection_attempt_postfix_fix` shares root's existing `InstantLiveTransport.swift` and focused `InstantLiveTransportTests.swift` claims. Test-first scope is synchronous/non-suspending session decoration, structural preservation of base attempt identity and abort ownership, public rejection of legacy sessions, duplicate-connect isolation, pre-factory cancellation, and named default timeout sleeps. Preserve every current transport/test hunk; parser/scoped diff only, no SwiftPM or commit.
- 2026-08-11T05:00:47-0400 synchronous-session-decorator-caller-split: the same worker also owns only the mechanically necessary synchronous mapping migrations in `InstantPlaybackRoomLiveValidation.swift` and `TypedAPITests.swift`. Playback will retain actor isolation through a lock-backed synchronous installation state; the typed local-only recorder becomes a lock-backed synchronous probe. Preserve every unrelated validation/typed-API hunk.
- 2026-08-11T05:10:32-0400 connection-attempt-postfix-source-freeze: the postfix slice is source-frozen. Session decorators are synchronous and prompt by type/contract; the arbitrary attempt-replacement combinator is deleted and its sole blocked-test caller explicitly composes the base attempt plus exact gate/base abort. Public `attempt.connect()` rejects an abort-incapable returned session. One-shot start ownership is decided before any caller installs a cancellation handler, so ordinary, already-cancelled, and concurrently cancelled duplicates cannot abort the winning caller; only the winner owns connector/session cancellation. `connectSession` checks cancellation before its factory and aborts a created attempt on every later failure. All three timeout entry points use `instantLiveDefaultTimeoutSleep`, including graceful close. Playback's actor delegates synchronous installation to a lock-backed state, and the typed local-only connect recorder is lock-backed. New exact tests: `connectionAttemptSourceKeepsDecoratorsSynchronousAndTimeoutDefaultsNamed`, `directAttemptRejectsLegacyReturnedSession`, `duplicateConnectAfterSuccessDoesNotAbortHealthySession`, `concurrentDuplicateConnectDoesNotAbortOriginalConnect`, `alreadyCancelledConcurrentDuplicateDoesNotOwnOriginalAbort`, and `alreadyCancelledConnectSessionSkipsAttemptFactory`; retain the existing mapped identity/capability tests. One frontend parse invocation for all four changed Swift files and scoped whitespace/search checks pass. No SwiftPM command or commit ran. Root owns serialized typechecking/testing.
- 2026-08-11T05:32:44-0400 connection-attempt-client-ownership-refreeze: the client-surface follow-up is source-frozen. State now returns an internal claimed-start value: `.duplicate` carries no cleanup authority, while `.owner` carries the exact one-shot async operation and synchronous abort boundary. Both public `attempt.connect()` and `connectSession` preserve the same public duplicate `InstantError`, but `connectSession` never enters its aborting catch for an ordinary or already-cancelled duplicate even when an invalid public factory reuses one attempt. A legitimate fresh attempt canceled inside its factory is claimed, synchronously aborted, and never starts connector work. Factory documentation explicitly requires a fresh attempt and states reused-attempt isolation. Exact regressions: `reusedAttemptFactoryDuplicateDoesNotAbortOriginalConnectSession`, `reusedAttemptFactoryCancelledDuplicateDoesNotAbortOriginalConnectSession`, and `cancellationAfterFreshAttemptFactoryAbortsOwnedAttempt`. Every new lifecycle `didStart`, cancellation completion, and connection completion wait is now inside `instantLiveWithTimeout(... 5_000)` with task cancellation on abandonment where applicable; the blocked transport helper is bounded too. All four connection/decorator Swift files pass one frontend parse invocation; scoped whitespace and stale-symbol searches pass. No SwiftPM command or commit ran; root owns the serialized focused gate.
- 2026-08-11T05:08:00-0400 infinite-query-setup-cancellation: `infinite_query_setup_cancellation` owns only auth/startup/pre-lease/setup/replacement cancellation sequencing in `InstantInfiniteQuery.swift`, cancellation-aware chunk-observation setup in `InstantRuntime.swift`, and focused blocked-setup tests in `InstantInfiniteQueryParityTests.swift`. It will make pre-install cancellation complete immediately, prevent late local/live registrations after cancellation, and remove unbounded same-key predecessor waits while preserving Runtime's reference-counted add/remove reconciliation. `infinite_query_terminal_cleanup` separately owns terminal graph release and eviction assertions. Parser/scoped diff only; no SwiftPM or commit.
- 2026-08-11T05:12:23-0400 infinite-query-setup-cancellation-source-freeze: source and four deterministic tests are frozen. `InstantLiveInfiniteSubscriptionSetupLease.cancel()` returns immediately before cleanup installation and a late installation cleans itself; starter setup no longer blocks on an auth read used only for diagnostics; same-key replacement no longer awaits retiring cleanup. Runtime checks cancellation around page-info and local-observer setup, installs the local observation lease before entering remote registration, uses the existing abort-capable five-second live-session send boundary, and cleans the lease on cancellation. Tests block before persisted page info, after local observer installation, and at the operation gate, then require stream termination plus zero late store observers/registered queries within exactly 5,000ms. The two Runtime default live timer sleeps now use the named stable sleep function. One frontend parse of all three shared Swift files and scoped whitespace checks pass; no SwiftPM command or commit ran.
- 2026-08-11T05:26:05-0400 public-infinite-subscribe-cancellation-source-freeze: the flawed test that awaited public subscription creation while deliberately holding its validation gate is replaced by exact already-cancelled and gate-queued caller-task contracts. `subscribeInfiniteQuery` returns an inert finished subscription on cancellation, never a validation error; validation distinguishes `CancellationError` from genuine synchronization failure, retaining the hot-store fallback only for the latter. The validation operation gate now removes a cancelled waiter and checks cancellation before returning synchronized attributes. Both tests require a finished empty stream, zero gate waiters, zero store observers/live query keys, and no late `add-query` after barrier release. Combined parser, scoped whitespace, and stale-symbol scans pass; no SwiftPM command or commit ran.
- 2026-08-11T05:31:00-0400 local-infinite-observation-lease: `infinite_query_setup_cancellation` adds a narrow exact-observer lease only in `InstantStore.swift` registration/cancellation, reuses it through local infinite-query setup and normal termination, and adds one deterministic cancellation-after-install test. Cancellation must return an inert finished subscription and synchronously remove the exact store observer within 5,000ms while the setup checkpoint remains blocked, then remain empty after release. Preserve every deferred-value/index/query edit; parser/scoped diff only, no SwiftPM or commit.
- 2026-08-11T04:10:47-0400 abort-test-fixture-source-freeze: the seven delegated scripted fixtures now use the four-closure session initializer with a synchronous lock-backed abort registry, one-shot checked-continuation boxes, and reusable pending-operation records. Every suspension-capable receive registers an exact abort action; both cancellation-insensitive send fixtures register an exact action too. `ImmediateAckBeforeSendReturnSession` aborts its send with cancellation, while `CloseDuringTransactLiveTransport` releases the send so it preserves its intended explicit-close network failure. Normal close synchronously aborts all registered operations before returning, and later sends/receives fail the shared abort check. The Core helper moved out of `InstantLiveTransportTests.swift` into `InstantReactorParityLiveSupport.swift`; the separate `InstantSwiftDataTests` target has its own shared copy. All seven files pass one Swift frontend parse and scoped whitespace checks. SwiftPM did not run. The only remaining verification risk is module-level typechecking and runtime behavior, owned by root's serialized focused gate; each fixture intentionally becomes terminal after abort, and current scripts use one session per actor while Bootstrap creates a fresh actor per connection.
- 2026-08-11T05:13:21-0400 infinite-query-terminal-cleanup-source-freeze: the terminal-retention slice is source-frozen. Live unsubscribe and hydration/setup failure capture the terminal sequence, schedule every live subscription retirement, then release forward/reverse payload chunks and keys, advanced/navigation cursor sets, the leading watcher, eviction/loading state, and prebootstrap hydration/navigation caches before preserving the existing finish or one-error-snapshot-then-finish semantics. Local failure and cancellation share one terminal helper that captures sequence first, then releases the unbounded latest emission, bounded hydrated values, hydration sequence, anchors, and paging state. A count-only package-local probe drives 10,000 entity snapshots through the real local coordinator and proves 10,000 latest values, 25 hydrated visible values, and one anchor become zero after both failure and cancellation; the failure still carries sequence 47 and the exact typed error. The live eviction regression now requires exactly one frozen query, exact ordered add/remove sequences including the second-query ABA cycle, the exact registered-query set, and zero local store observers after unsubscribe within 5,000ms. Both shared files pass one frontend parse invocation and scoped whitespace checks. No SwiftPM command or commit ran. Exact focused tests for root: `terminalCleanupReleasesTenThousandRowLocalGraphAfterFailureAndCancellation` and `liveWindowEvictsAndCancelsOppositeCursorChunkInBothDirections`, then the complete `InstantInfiniteQueryParityTests` suite.
- 2026-08-11T05:36:20-0400 infinite-query-terminal-retention-postfix: after `infinite_query_setup_cancellation` froze its exact local observation lease, `infinite_query_terminal_cleanup` resumes only the live prebootstrap expansion scheduler/terminal graph, the local post-install primary/navigation task ownership boundary, clean terminal buffer replacement, two adjacent package test hooks in `InstantRuntimeConfiguration`, and isolated 10,000-row public-subscription regressions. It will preserve `InstantStoreQueryObservationLease`, its awaitable cancellation, every setup checkpoint, and the four frozen setup/public-cancellation tests. Parser/scoped diff only; no SwiftPM or commit.
- 2026-08-11T05:55:21-0400 infinite-query-terminal-retention-postfix-source-freeze: postfix source/tests are frozen. Live prebootstrap expansion has one actor-owned cancellable task, a generation/terminal check after every suspension, no diagnostics-only auth await, and a count-visible payload checkpoint; terminal cleanup cancels the task and cannot be considered empty until the task releases its acquired values. Local public subscriptions use one owner with exactly one primary task, one navigation task, and one latest pending direction scalar; 10,000 alternating next/previous calls while hydration is blocked remain at two owned tasks plus one scalar. Clean unsubscribe replaces `AsyncStream.bufferingNewest(1)` payloads with one empty sequence-preserving terminal snapshot before finish; failure retains the existing empty typed-error snapshot. Output stream/continuation scalars are split and onTermination is continuation-free/weak-owner; the exact store observation lease remains awaitably canceled and nilled once. Exact tests: `terminalCleanupReleasesTenThousandRowLocalGraphAfterFailureAndCancellation`, `unsubscribeEvictsUnconsumedTenThousandRowSnapshotBuffer`, `livePreBootstrapCancellationReleasesBlockedTenThousandRowExpansion`, `localNavigationCancellationReleasesBlockedTenThousandRowRequest`, and `liveWindowEvictsAndCancelsOppositeCursorChunkInBothDirections`. One frontend parse across all three files and scoped `git diff --check` pass; no SwiftPM command or commit ran. Source hashes: `InstantInfiniteQuery.swift` 8f321918a9220d08749916636af2f7486283328387be6a1f3793a699b2fe5e5a, `InstantRuntime.swift` 6aa1c794b9615c0831794bbb502e5286d23292aa86b2a810cc857656ff4f2e62, tests b2b86eb4ca9b4dcff8f7d2b1c7b129e4acc17864d462cdd1dc48008e06d7209c.
- 2026-08-11T06:55:53-0400 standard-query-exact-lease: `standard_query_exact_lease` owns only the public/standard observation APIs in `InstantStore.swift`, the standard observation path in `InstantRuntime.swift`, and focused standard lifecycle tests in `InstantLiveTransportTests.swift`. The goal is one exact Store lease carried through local/live wrapping and awaitably canceled before remote unregister completes. This slice explicitly does not touch infinite chunk setup or `hydratingDeferredValues`; `deferred_hydration_exact_lease` owns that separate region. Tests first; parser/scoped diff only; no SwiftPM or commit.
- 2026-08-11T06:58:22-0400 standard-query-exact-lease-source-freeze: the standard observation slice is frozen. `InstantStore.observeQueryLease` preserves the exact registered observer identity while the public Store API remains stream-shaped. Runtime retains that lease for both local and live standard observations. Local termination awaits its exact Store cancellation; live termination awaits the same cancellation before releasing live-result ownership and beginning remote unregister. The deterministic `standardLiveObservationCancelsExactStoreLeaseBeforeBlockedRemoteUnregister` regression repeats consumer cancellation, blocks the exact `remove-query`, and requires zero Store observers/query keys while the remote removal is still suspended, then requires exactly one removal after release. This proves cleanup ordering without polling deinit. One frontend parse across the three owned Swift files and scoped whitespace checks pass. No SwiftPM command or commit ran. Exact filter: `swift test --jobs 1 --no-parallel --filter standardLiveObservationCancelsExactStoreLeaseBeforeBlockedRemoteUnregister`.
- 2026-08-11T07:02:11-0400 standard-query-hydration-lease-refreeze: after `deferred_hydration_exact_lease` landed its exact hydration lease, both standard local and live call sites now wrap the hydrated stream in one final idempotent observation lease. Final cancellation concurrently awaits hydration task release and the underlying observation cancellation; the underlying live path still removes the exact Store observer before remote unregister begins. Every test wait is cancellation-cooperative and bounded at exactly 5,000ms: scripted sent-count and remove-entry waits use the shared hard timeout, and consumer completion now has its own hard timeout plus cancellation abandonment. Frontend parse and scoped whitespace checks pass again. No SwiftPM command or commit ran. Current shared-file hashes: Store `91c83935ee0b0eb5193fce3dff0e9a01692efc5e393941d752afd043edaa6d69`; Runtime `1caf1a8d01e829ac0e5d3ef672ccb25c3f8dc8d1e65c663fc9bd919f0e9186a9`; live tests `50c6e14319f8c7ceff3842f6a1a5d3f23c8efc34f535debdf8c9a17151f36c55`. Exact filter remains `swift test --jobs 1 --no-parallel --filter standardLiveObservationCancelsExactStoreLeaseBeforeBlockedRemoteUnregister`.
- 2026-08-11T07:05:15-0400 standard-query-test-autoclosure-refreeze: focused package compilation found only Swift Testing autoclosure isolation errors in the new regression. The four Store actor reads are now awaited into named values before `expectNoDifference`; the expectations and lifecycle behavior are unchanged. The test file passes frontend parsing and scoped whitespace checks. No SwiftPM command or production edit ran.
- 2026-08-11T07:00:00-0400 deferred-hydration-exact-lease: `deferred_hydration_exact_lease` owns only the `hydratingDeferredValues` helper and its composition into `observeLiveInfiniteQueryChunk`, plus one adjacent deterministic blocked-hydration retirement test. It will make deferred hydration return an exact async-cancel lease, await the hydration forwarding task, and compose hydration plus underlying observation cleanup exactly once. Preserve all store, standard-query, setup-cancellation, retirement, and terminal-retention edits; parser/scoped diff only, no SwiftPM or commit.
- 2026-08-11T07:01:39-0400 deferred-hydration-exact-lease-source-freeze: deferred hydration now returns an exact stream/cancel lease. Cancellation finishes the output, cancels and awaits the hydration task, and each infinite chunk composes hydration and underlying Store/live cancellation concurrently behind one idempotent termination. The deterministic deferred-payload fixture blocks after acquiring one hydrated row, requires unsubscribe to retain one retiring slot and three owned tasks while the exact hydration task is blocked, requires the Store observer to be removed concurrently and cleanup to run once, then requires zero residency and observers after release. Standard query call sites were adapted separately by `standard_query_exact_lease`. Both files pass one frontend parse and scoped whitespace checks; no SwiftPM command or commit ran. Exact filter: `blockedDeferredHydrationKeepsExactRetirementOwnedUntilLeaseRelease`.
- 2026-08-11T07:08:22-0400 standard-observation-residual-lifecycle: `standard_observation_lifecycle` owns only standard non-infinite setup/lease sections of `InstantStore.swift` and `InstantRuntime.swift`, standard Fetch/public cancellation ownership in `InstantSwiftData.swift`, and focused standard lifecycle additions to `InstantLiveTransportTests.swift`. It preserves the frozen infinite-query and deferred-hydration work. Test-first contracts: cancellation while queued at validation and after exact Store installation but before remote registration returns an inert finished observation with zero Store observers/live keys and no late add-query; one reusable idempotent async cancellation owner awaits Store removal, deferred-hydration completion, and remote unregister before public completion; cleanup releases retained resources after completion; repeat cancellation remains exact. Every bounded wait is cancellation-cooperative and exactly 5,000 milliseconds. Parser/scoped diff only; no SwiftPM, commit, or unrelated edit.
- 2026-08-11T07:08:22-0400 standard-observation-public-fetch-test-split: the same worker additionally owns new `Tests/InstantSwiftDataTests/StandardObservationLifecycleTests.swift` because the Core-only live transport target cannot exercise the public `FetchSubscription` cancellation boundary. This one focused file will prove public completion awaits exact upstream cleanup, repeated cancellation is idempotent, and a retained canceled handle no longer retains its completed cleanup capture graph. No existing `TypedAPITests.swift` ownership changes.
- 2026-08-11T07:08:40-0400 infinite-subscription-residual-lifecycle: `infinite_subscription_lifecycle` shares the existing plan/claims and owns only `InstantInfiniteQuery.swift`, infinite-only raw/typed handle sections in `InstantTypedAPI.swift`, and focused infinite lifecycle tests. It preserves every frozen setup, deferred-hydration, standard-observation, and terminal-retention hunk. Test-first contracts: local exact cleanup joins primary/navigation tasks plus the Store lease behind one idempotent cleanup task while publishing the terminal snapshot immediately; live commands use one bounded mailbox runner plus one coalesced navigation scalar and terminal priority; raw/typed canceled handles release coordinator, runtime, and upstream resources through one reusable reference owner. Exact blocked-task, 10,000-command, no-late-navigation, and weak-lifetime regressions use cancellation-cooperative 5,000-millisecond bounds. Parser/scoped diff freeze only; no SwiftPM, commit, or unrelated edit.
- 2026-08-11T07:15:40-0400 infinite-public-task-overlap: root authorizes `infinite_subscription_lifecycle` to share `InstantSwiftData.swift` only in `InfiniteQuerySubscription<Element>` and immediately adjacent infinite-specific initializer/task plumbing. `standard_observation_lifecycle` retains `FetchSubscription`, `FetchSubscriptionCancellation`, all standard fetch helpers, and writes first; both workers are coordinating the reusable nullable async cancellation-owner API before either expands. No simultaneous shared-file patching.
- 2026-08-11T07:26:48-0400 standard-public-fetch-api-source-freeze: the generic/standard `InstantSwiftData.swift` hunk is source-frozen and released for the coordinated infinite-only struct patch. `FetchSubscriptionCancellation(cancelAndWait:)` provides synchronous request-only idempotent `cancel()` plus exact `wait() async`; it takes and nils the operation before starting one terminal task, resumes waiters only after async cleanup returns, and retains neither a completed task nor the cleanup capture graph. `FetchSubscription` swaps its reference-backed stream storage to nil only after exact cleanup, so a retained canceled handle does not retain an unconsumed buffered payload. Standard runtime-backed subscriptions carry the exact query lease; the bare `observe`/custom-client stream remains an explicit compatibility boundary. All five scalar/snapshot adapters route through one lease-aware helper, while `map` and `combineLatest` await their forwarding tasks and upstream cancellations. Frontend parse, scoped whitespace, and the no-direct-`client.observe` scan pass. Hash before the infinite-only patch: `InstantSwiftData.swift` 38fa665304d1aaa0a7366c0ed8383d190f5a67cbe2ccafdcc8d6cbcb31dce513. No SwiftPM command or commit ran.
- 2026-08-11T07:40:32-0400 standard-observation-residual-lifecycle-final-source-freeze: all standard Store/Runtime/public Fetch source and focused tests are frozen. One package `InstantAsyncCancellationOwner` is created before setup suspends, atomically orders cancellation against late cleanup installation and `completeSetup`, runs cleanup outside its lock, nils the operation before its one terminal task, resumes exact waiters only after cleanup, and retains no completed task/action graph. Runtime returns a query lease sharing that owner; cancellation before the handoff exact-cancels and returns a finished lease. Standard Fetch setup, request mapping, and sequential two-source composition inject that same precreated owner into the returned subscription; a second-source failure exact-cancels the first. Manual and automatic property-wrapper tasks, `map`, and `combineLatest` await forwarding plus upstream cleanup. The reference-backed Fetch stream is released only after exact cleanup, including its buffered payload. Gate-queued and post-Store/pre-register cancellations return finished with zero Store/live registration state and no late add-query. Exact focused tests: `standardLiveObservationCancelsExactStoreLeaseBeforeBlockedRemoteUnregister`, `cancellingStandardObservationQueuedAtOperationGateReturnsFinishedWithoutLateRegistration`, `cancellingStandardObservationAfterStoreInstallationCleansBeforeRegistration`, `publicFetchCancellationWaitsForExactStoreCleanupAndRunsOnce`, `publicFetchTaskCancellationWaitsForExactStoreCleanup`, and `retainedCanceledFetchHandleReleasesCompletedCleanupCaptureGraph`. Core/public/test frontend parses, scoped whitespace, stale direct-observe/synchronous-defer scans, and infinite-cleanup placement checks pass. No SwiftPM command or commit ran. Hashes: Store `c76ed58a9fb68af75d8b584bfb2f76a84388c783cd83c95ad32bd182141c34e0`; Runtime `7b806948c449e00be02e5485d2c6bace831b2be5b67efb680ec82cddd549eb21`; shared public file `54ec2b275c600608385c2122491ccc5c29c7d9b55318edc56996c6d1585c2335`; Core tests `53b216e2b7266ee3c174b1bcd99076d4c1b47083a057d5a7bb325bb960aa55cb`; public tests `db690461a9b11967012dbb105e5f37246e4364987975f30e8464fd13d7513a34`.
- 2026-08-11T07:42:55-0400 standard-observation-cleanup-capture-refreeze: final retention review tightened the shared owner’s completion order. Its terminal Task now captures only a clearable operation box; the box nils its stored action before awaiting, nils the task-local action after exact cleanup, returns, and only then resumes cancellation waiters. Store and Runtime termination wrappers now delegate to that same owner instead of retaining actor-local completed Task handles, so no concurrent waiter can return while a stored completed Task still owns the cleanup graph. Combined Core/public/test frontend parses and scoped whitespace checks remain green; the shared public/infinite hash is unchanged and the infinite placement scan remains valid. No SwiftPM command or commit ran. Updated hashes: Store `515a8a57e05c4752c6d58607d317940601033a4f2aa0fc16dc913fcd508611e3`; Runtime `8d7fc9c221605c153809273947ee2e4212d3c3979c17b04bd7d0a47c7f7eae0c`; public `54ec2b275c600608385c2122491ccc5c29c7d9b55318edc56996c6d1585c2335`; Core tests `53b216e2b7266ee3c174b1bcd99076d4c1b47083a057d5a7bb325bb960aa55cb`; public tests `db690461a9b11967012dbb105e5f37246e4364987975f30e8464fd13d7513a34`.
- 2026-08-11T07:46:35-0400 standard-observation-core-test-compile-refreeze: root’s combined SwiftPM compile reached the focused Core tests and identified only three compile-only fixture issues. The two throwing temporary-cache URL calls now use `try`, and the cancellation-cooperative `Void` setup barrier explicitly types its raw throwing continuation as `CheckedContinuation<Void, any Error>`. The Core test file passes frontend parsing and scoped whitespace checks; no SwiftPM command was run by this worker. Updated Core test hash: `9cd1001be27a8e4e4220187c7e8cc1d3d0db07265416e50fc91f5a82f0985e05`.
- 2026-08-11T07:49:19-0400 standard-observation-store-probe-visibility-refreeze: the combined compile reached `StandardObservationLifecycleTests` and showed its six exact Store-residency assertions could not access the existing internal observer-count probe across package targets. Only `InstantStore.activeObservationCount()` is widened to `package`; the assertions and runtime surface remain unchanged. Store plus the focused public test pass frontend parsing and scoped whitespace checks; no SwiftPM command ran. Updated Store hash: `4aeb961e5d7d68e619a4a08f5ddefe549401f788171e90d6ac902c003b580a92`.
- 2026-08-11T07:41:12-0400 infinite-subscription-residual-lifecycle-final-source-freeze: the infinite lifecycle slice is source/test frozen. Raw and typed commands capture only one `InstantInfiniteQueryCommandOwner`; active navigation uses one runner plus one latest-direction scalar, terminal transition rejects late commands, and one fresh uncanceled terminal task runs exact resource cleanup concurrently with the canceled runner join before clearing resources/task slots. Local termination publishes its empty sequence-preserving terminal snapshot immediately, then one memoized cleanup task removes the exact Store lease, abandons the coordinator, and joins cancellation-insensitive primary/navigation tasks; the nullable Store/onAbandon resource and every task slot are cleared after completion. Retained canceled raw/typed handles therefore keep only completed small owners, not Runtime/coordinator/upstream/buffered-stream graphs. The infinite property-wrapper helper requests cancellation and awaits the subscription's exact `.task` on every exit. Exact focused filters: `localInfiniteCleanupJoinsBlockedPrimaryTaskAfterImmediateTerminalPublication`, `localInfiniteCleanupJoinsBlockedNavigationTaskWithoutLateNavigation`, `liveInfiniteCommandOwnerCoalescesTenThousandCallsAndPrioritizesTerminalCleanup`, `liveInfiniteTerminalCleanupCanReleaseBlockedNavigationBeforeJoiningRunner`, `retainedCanceledRawInfiniteHandleReleasesCoordinatorAndRuntime`, and `retainedCanceledTypedInfiniteHandleReleasesRawUpstreamAfterExactTaskCompletion`. One combined frontend parse and scoped `git diff --check` across the five owned source/test files pass. Static scans show exactly one `cancellationRequested` declaration, and `subscriptionForCleanup` is declared/used only inside `runInfiniteQueryStorageSubscriptionTask<Element>` with no generic Fetch overlap. No SwiftPM command or commit ran. Hashes: Core source `8fb842f9259ad4eaef3238063f2184e01667ac006ff041823970662e4bdf32bb`; shared public source `54ec2b275c600608385c2122491ccc5c29c7d9b55318edc56996c6d1585c2335`; typed source `b63e4fbff795241e49d29f1714bd9ae3c237c6cadc220df68bacb096759c1754`; Core tests `a5248ce7d577d0502a3e1df64de7dd6fe32588ea89585a045b82b96a93dab0df`; typed tests `68249081f88c4cf48d44d188f37ed3a3721a48b7d84537094f1951a3d80feefc`.
- 2026-08-11T07:42:29-0400 infinite-subscription-compile-shadowing-refreeze: the combined SwiftPM compile exposed one local-name shadowing error before tests: the optional binding inside `completeTerminalCleanup` resolved to the outer local being declared rather than the owner's stored cleanup task. The binding now explicitly reads `self.cleanupTask`; behavior and every other hunk are unchanged. Combined five-file frontend parse and scoped `git diff --check` pass. No SwiftPM command, test, commit, or unrelated edit ran by this worker. Updated Core source hash: `578c50898263809c0af9bebff3d976a5ae6578aa3e2793db8da8c2fab2c31b21`; all other previously frozen hashes are unchanged.
- 2026-08-11T08:02:00-0400 standard-observation-gate-fixture-correction: the authoritative 12/13 focused run exposed one fixture collision, not a setup-owner defect. `connect()` schedules an unrelated unstructured startup mutation pump; when prune owns the operation gate, the test can mistake that pump for the named standard-observation waiter, then correctly cancel the observation and still see the pump as the one remaining total waiter. `standard_observation_lifecycle` retains its existing test-only claim and will keep the strict total-zero waiter assertion by exercising the same live-registration gate path with a configured but intentionally disconnected live transport. This removes the startup pump from the fixture, retains zero Store/query/live-key checks, and proves no late `add-query` after gate release. No production ownership expands; parser/scoped diff only, no SwiftPM or commit.
- 2026-08-11T07:59:34-0400 standard-observation-gate-fixture-refreeze: the sole failing focused test is corrected and refrozen without a production edit. The fixture no longer calls `connect()`, so the configured live transport still makes standard setup enter the named live-query operation gate, while no unrelated startup mutation pump can queue behind the prune barrier. Cancellation must still return a finished observation with total gate waiters zero, Store observers zero, Store/live query keys empty, and the scripted wire log still empty after gate release, proving no late `add-query`. `swiftc -frontend -parse` and scoped `git diff --check` pass; no SwiftPM command or commit ran. Updated Core test hash: `fa5b457681d62111c4b6f05125dd4d0ea26cdda4465400e8fe254ca5908bd1c6`. Runtime remains `8d7fc9c221605c153809273947ee2e4212d3c3979c17b04bd7d0a47c7f7eae0c`; Store remains `4aeb961e5d7d68e619a4a08f5ddefe549401f788171e90d6ac902c003b580a92`; public remains `54ec2b275c600608385c2122491ccc5c29c7d9b55318edc56996c6d1585c2335`; public tests remain `db690461a9b11967012dbb105e5f37246e4364987975f30e8464fd13d7513a34`.
- 2026-08-11T08:05:25-0400 infinite-exact-live-cleanup-follow-up: `infinite_subscription_lifecycle` reclaims only `InstantInfiniteQuery.swift`, focused `InstantInfiniteQueryParityTests.swift` cleanup/watchdog assertions, and the stale `TypedAPITests.swift` window-navigation fixture. The typed fixture will serialize next/previous counter waits without changing the intentional one-latest-direction command scalar. Test-first live scope is an actor-owned exact cleanup completion condition: synchronous unsubscribe still publishes terminal immediately, while `unsubscribeAndWait` remains pending until active subscriptions, retiring slots, exact Store observers, and prebootstrap expansion are truly empty; the five-second watchdog records but cannot abandon real cleanup or hide residency. `clean_unsubscribe_watchdog_failure` owns the current failure diagnosis/test overlap and `live_unsubscribe_exact_wait_design` owns read-only design review; production architecture remains provisional until both final messages arrive. Parser/scoped diff only; no SwiftPM, commit, polling, hidden timeout loser, or unrelated edit.
- 2026-08-11T08:09:43-0400 infinite-exact-live-cleanup-follow-up-source-freeze: both read-only investigations converged on the same closed-set task snapshot and the follow-up is source/test frozen. Live `unsubscribeAndWait` first calls synchronous `unsubscribe`, which marks the coordinator inactive, moves every active subscription into retirement, publishes the empty terminal snapshot, and finishes the stream immediately. It then snapshots the post-termination retirement cleanup tasks plus the prebootstrap expansion task and awaits their nonthrowing values; it does not await or treat the five-second watchdog as cleanup. Inactivity prevents new retirement or expansion work, each cleanup task joins its exact setup/Store/live lease and removes its actor slot, and the coordinator remains strongly reachable through the command owner's resource operation until all exact tasks finish. The post-start cancellation branch uses the same exact method. The clean watchdog regression starts a repeated exact waiter, proves it remains incomplete while watchdog residency stays visible at active 0 / retiring 1 / owned tasks 2 and the Store observer remains installed, then proves completion and zero residency/Store/live registrations only after barrier release. The typed window fixture now awaits the next-page counter before issuing previous-page, preserving intentional latest-wins coalescing. Combined five-file frontend parse and scoped `git diff --check` pass; no SwiftPM command, test, commit, polling, hidden timeout task, continuation waiter, or unrelated edit ran. Exact filters: `cleanLiveUnsubscribeReportsStalledRetirementUntilLeaseRelease` and `typedInfiniteQueryForwardsWindowRetentionAndPreviousPageAction`. Hashes: Core source `7e66f759c27ca8c659a69b8cb856ebeaa218dcd32dda132d9bffb003d843e97a`; Core tests `71c89c97d2f03343f5e48c6854fad080b031b8f6f193416b3e377876bfd5613b`; typed tests `b30cc34792eaba7242d8bcd0cf8bbd107ae91bc6f19991a493b284a512e5a3e6`.
- 2026-08-11T08:13:17-0400 deferred-hydration-cleanup-hook-race-refreeze: a fresh-process 10-run stress exposed only a scheduler-order assumption in `blockedDeferredHydrationKeepsExactRetirementOwnedUntilLeaseRelease`: the retirement slot and its three owned tasks can become visible before the cleanup task begins its `onCancellationStarted` hook. The direct cleanup-probe read now uses the existing cancellation-cooperative `instantLiveWithTimeout` pattern, checking cancellation and yielding until the count reaches one within exactly 5,000 milliseconds. All exact retirement residency and Store-observer assertions remain unchanged. The nearby direct hook-count reads are causally ordered by completed install, exact waiter, or zero-residency completion; no analogous unsynchronized assertion was found. Focused frontend parse and scoped `git diff --check` pass; no SwiftPM command, test, production edit, commit, or unrelated edit ran. Updated Core test hash: `58019742702e348d0af8e263afd19727f9df62778a99b4bd3611e89a9bee97d9`.
- 2026-08-11T09:48:57-0400 bounded-corruption-fixture-ordering: the definitive clean suite exposed one raw-SQL fixture race in `mixedCorruptWindowRefillsBeforeTheValidRowsAreAcknowledged`. The preserved database proves the live pump claimed the first 50 rows before the corruption helper's first write: 51 valid rows, 50 under one claim token, zero malformed bodies. Root retains its existing bounded-outbox test claim and will correct that test plus the two latent identical fixtures to bootstrap disconnected, seed/corrupt/invalidate, then connect inside the existing known-issue scope. Exact five-second send gates and delivery assertions remain unchanged; no SQLite busy retry or production behavior change is authorized by this slice.
- 2026-08-11T09:55:26-0400 exact-mutation-pump-close-investigation: the same clean suite recorded a late `disk I/O error` against the bootstrap test's already-removed temporary database after that test had passed and called `closeConnection`. Root joins the existing Runtime and Bootstrap claims for a test-first exact-lifetime correction. Provisional boundary: `startLiveMutationDeliveryIfNeeded` creates an unowned task, while `closeConnection` neither disables nor joins it; the coalescing actor tracks only booleans, not the task. Two independent read-only reviews are tracing close/connect/reconnect ordering before source changes. A fixture-only idle poll is explicitly rejected unless those reviews disprove the production lifetime gap.
- 2026-08-11T10:27:34-0400 exact-runtime-close-lifecycle: root delegates `exact_close_production` only `Sources/InstantSwiftDataCore/InstantRuntime.swift` and this existing Runtime claim/channel bookkeeping. It preserves every dirty hunk and implements one reusable tokenized restartable exact task owner for bootstrap automatic connection and startup cookie synchronization, exact reconnect and receiver ownership, stop-before-gate ordering, closed-state persistence under the operation gate, and concurrent exact joins outside that gate while the connection gate remains held. Package-only probes/hooks expose deterministic auto-start, cookie-start, receiver-event acquisition, and aggregate close-idle state. Tests remain root-owned; this worker runs parser/scoped diff checks only and no SwiftPM or commit.
- 2026-08-11T10:40:12-0400 exact-runtime-close-lifecycle-source-freeze: production source is frozen at SHA-256 `29029421b197b11703bf819a272584e919e01250e066c762d17519841683e0bd`. One lock-linearized tokenized owner now retains automatic connection, startup cookie synchronization, and coalesced reconnect work through exact completion; the live-session receiver uses the same owner across wire acquisition, record, Runtime event handling, `receiverEnded`, and failure handling, and replacement open joins the old generation before opening another session. Explicit close latches automatic connection, cookie sync, reconnect, and mutation delivery before the connection gate; begins receiver close under the operation gate; persists and publishes `.closed`; releases the operation gate; then concurrently joins all five exact handles while retaining the connection gate on both success and error. A separate injected 5,000-millisecond watchdog only reports named non-idle owners and continues exact waiting; its own task is canceled and joined before return. Auto cancellation cannot schedule reconnect; startup cookie cancellation propagates and is checked before its final metadata save. The three package hooks and aggregate idle probe match the focused tests. One frontend parse and scoped whitespace checks pass; no SwiftPM command, test edit, commit, or unrelated source edit ran in this worker.
- 2026-08-11T10:28:01-0400 exact-runtime-close-tests: root delegates `exact_close_tests` only `BootstrapTests.swift`, `InstantReactorParityTests.swift`, and `InstantReactorParityLiveSupport.swift`, plus these claims. It preserves all dirty test work and adds deterministic cancellation-insensitive externally released barriers for automatic connection, startup cookie synchronization, live receiver event acquisition, and the upstream reconnect-close parity fixture. Every outer wait is bounded at exactly 5,000 milliseconds; close must remain pending until each owned cleanup path is externally released, then prove aggregate exact-background idle state and zero gate waiters. Production remains `exact_close_production` owned. Parser/scoped diff only; no SwiftPM or commit.
- 2026-08-11T10:38:44-0400 exact-runtime-close-tests-source-freeze: the three owned test files are frozen against the production API. `closeConnectionCancelsAndJoinsBlockedAutomaticLiveConnectionTask` blocks before connection work and proves durable closed state plus zero connection requests while close remains incomplete; `closeConnectionCancelsAndJoinsBlockedStartupCookieSyncTask` proves the same boundary with zero cookie requests; `closeConnectionCancelsAndJoinsBlockedLiveReceiverEvent` blocks a real `transact-ok` after acquisition and proves the mutation stays pending with no extra connection before and after exact receiver join. `upstreamPythonConnectionCloseCancelsInflightReconnectTask` now blocks inside post-cancellation reconnect cleanup, so closed state is visible while close remains incomplete until external release. One reusable lock-backed two-stage checkpoint records actual task cancellation without cooperating with it, and reconnect count waits use cancellation-cooperative polling instead of retained continuations. Every new outer wait is exactly 5,000 milliseconds. Final frontend parse and scoped whitespace checks pass; no SwiftPM command, test, commit, or unrelated edit ran. Hashes: Bootstrap tests `084f8e2ee010332b55af287e89b9abd2b76b8e020c0e0c9c778cbeee13f055d9`; Reactor parity tests `113d33e8c87ba868e7cf46f216c3679efdac82e192077c602065745946b837f8`; live support `58d8710bfe024e012c865886eef4b3972249e83e507852a000e349fd80acf24d`.
- 2026-08-11T14:47:05Z cumulative-stream-snapshot-buffer: root delegates `stream_snapshot_buffer` only `InstantSnapshotObservers.swift` and the stream-observation section of the already shared `InstantReactorParityTests.swift`. The finished exact-close and parity hunks remain frozen. Test-first scope is one deterministic cumulative stream-read contract: publish several complete growing snapshots before consumption, retain only the newest complete value and its exact offset/size/update metadata, then replace the stream-content observer's unbounded buffer with one newest snapshot. Parser/scoped diff only; no SwiftPM, commit, Runtime edit, or unrelated test edit.
- 2026-08-11T10:46:56-0400 query-once-live-exact-cleanup: root delegates `query_once_exact_cleanup` only the `queryOnceThroughLive` cleanup hunk in `InstantRuntime.swift` and one focused exact-cleanup regression block in `InstantLiveTransportTests.swift`. The previous Runtime and test owners are inactive or source-frozen; this slice preserves every concurrent hunk. It will replace the defer-spawned unowned cleanup with the existing exact cancellation-owner/lease architecture and prove success, failure, and cancellation cannot return while live-result release or query unregistration is externally blocked. Every outer wait is exactly 5,000 milliseconds. Parser/scoped diff only; no SwiftPM, commit, fixed sleep, or unrelated edit.
- 2026-08-11T10:48:30-0400 query-once-live-result-probe-split: root authorizes `query_once_exact_cleanup` to additionally claim only one package testing probe on `InstantLiveQueryResultState` in `InstantLiveRefreshApplication.swift`, plus its Runtime wrapper. The exact regression must prove the actual live-result retained-key graph reaches zero after blocked removal, not infer it from cleared page information. Every existing live-refresh translation, replacement, and residency hunk remains untouched.
- 2026-08-11T10:55:01-0400 query-once-live-exact-cleanup-source-freeze: the one-shot live-query cleanup slice is source/test frozen. `queryOnceThroughLive` retains its result state, installs one existing `InstantAsyncCancellationOwner`, and on success, thrown error, or caller cancellation requests exact cleanup and awaits it before returning; cleanup releases the result-state key then awaits live query unregistration, with no defer-spawned task. A package-only post-acknowledgement hook creates a deterministic still-connected error path. One parameterized regression drives success, that injected error, and cancellation into a real blocked `remove-query`; in every case the public call remains incomplete while live membership, result-state active keys, and Store observers are already zero, then completes only after external release and reasserts zero final residency. The existing removal-race transport gained only queued server-message support. Every new outer wait is exactly 5,000 milliseconds and uses no fixed sleep. All three changed Swift files pass frontend parse and scoped whitespace checks; the queryOnce hunk contains no `Task` or async defer cleanup. No SwiftPM command or commit ran. Exact filter: `queryOnceWaitsForExactCleanupBeforeEveryTerminalPath`. Hashes: Runtime `074dc3cb5b26793ea496a4c7cf3d14d8f82ef74617d51e89a4044976a0478279`; live-result state `f934d65e7ff75ed6ca2547084ee77c8e37870e0d4409e42e662bc78f0e8cf65d`; live transport tests `827d5eb0f05e7ab2717c52c16af496d5668224b7283e27b35ab81c27cd7b4db1`.
- 2026-08-11T10:56:04-0400 query-once-test-throwing-url-refreeze: root's pre-SwiftPM compile-risk review found the new parameterized fixture omitted `try` on the existing throwing temporary-cache URL helper. That sole call now uses `try`; test frontend parse and scoped whitespace checks pass. Runtime and live-result-state source are unchanged. Updated test hash: `a55f855b5f43b52df71d50f962c22d73fc8ae65399d8ac43a58772878a87cca5`.
- 2026-08-11T10:49:27-0400 cumulative-stream-snapshot-buffer-source-freeze: source and focused test are frozen. `InstantStreamContentObservers` now uses `bufferingNewest(1)`, matching its values' complete cumulative-snapshot semantics and preventing an inactive reader from retaining every superseded payload. Deterministic test `streamContentObserverRetainsOnlyNewestCompleteSnapshot` publishes three growing reads before creating an iterator and compares the first retained value to the full final read, including content, byte offset/count, metadata size/update sequence, and done state. Combined frontend parse and scoped whitespace checks pass. No SwiftPM command, commit, Runtime edit, fixed sleep, or unrelated test edit ran. Hashes: observer source `ab770f4c9ab929c562c40743105af5d597a37f3284a1135b2a57aaa60f4c4d46`; Reactor parity tests `93c407657d8eebed3a660ecd7f99f0762e04364694a5bf045394ee9a801bf75e`.
- 2026-08-11T14:50:49Z explicit-flush-bounded-exact-tests: root delegates `explicit_flush_tests` only `InstantBoundedOutboxDeliveryTests.swift` plus this existing claim/channel. The slice replaces the detached-late-finish expectation with deterministic red contracts for one bounded explicit-flush window (50 decoded bodies, 256 steps, 8 MiB, smaller caller limit), timeout that synchronously aborts then remains pending until cancellation-insensitive transport cleanup is released, exact idle ownership afterward, and close that aborts/joins a blocked explicit flush with no post-close send or write. Every outer wait is exactly 5,000 milliseconds; preserve all existing dirty hunks; frontend parse/scoped diff only, no SwiftPM, production edit, or commit.
- 2026-08-11T14:57:50Z explicit-flush-bounded-exact-tests-source-freeze: test source is frozen at SHA-256 `9ffb88210e8f519fe052a3595a62bc8e6740861375e98df4844bcc366454a934`. `unlimitedExplicitFlushOfTenThousandRowsAdmitsOneFiftyMutationWindow`, `unlimitedExplicitFlushHonorsTheTwoHundredFiftySixStepWindow`, and `unlimitedExplicitFlushHonorsTheEightMiBDurableBodyWindowBeforeDecode` require nil-limit selection to stop at one 50-body / 256-step / 8-MiB window, leave every unselected tail row ready and never offered, and carry selected bodies through disposition without a second decode; the existing 10,000-row `limit: 1` test now requires exactly one decode. `explicitFlushTimeoutAbortsAndExactlyJoinsCancellationIgnoringOperationAndRenewal` replaces the old detached-late-finisher contract: a deterministic five-second deadline synchronously aborts an `InstantMutationTransportOperation`, waits for both its cancellation-insensitive accepted-response tail and a cancellation-insensitive renewal sleep, discards the late acceptance, releases the row ready, and returns only once the existing aggregate exact-close idle probe is true. `closeConnectionAbortsAndExactlyJoinsActiveExplicitFlushBeforeReturning` requires closed state to become visible while close remains blocked, then exact-joins transport/deadline/renewal, releases the row before close returns, and proves the flush caller cannot change the outbox revision afterward. Frozen package API expectations are `InstantMutationTransportOperation(run:abort:)`, `InstantMutationTransportClient.preparedOperations`, `InstantRuntimeConfiguration.explicitMutationTransportDeadlineSleep`, `InstantRuntimeConfiguration.explicitMutationClaimRenewalSleep`, and inclusion of explicit-flush ownership in `exactCloseBackgroundTasksAreIdleForTesting()`. One frontend parse and scoped whitespace check pass; no SwiftPM command, production edit, commit, fixed sleep, or unrelated edit ran.
- 2026-08-11T15:01:21Z explicit-flush-authoritative-late-response-correction: corrected test source is refrozen at SHA-256 `53f2046890ee3d86a7a17cd01bbe2d1084fb2d235286b247aa8af9b34b107055`. The prior discarded-response expectation was unsafe because synchronous abort cannot prove the server did not accept. Timeout now requires the five-second deadline to complete naturally, transport abort while renewal remains active, exact late `.serverAccepted` disposition to durable `.confirmed` / `.serverTransport`, renewal cancellation only after that disposition, and no outbox revision change after the timeout error returns. Close now aborts transport and cancels its deadline while renewal remains active and connection state remains non-closed; the late accepted response must become durably confirmed before renewal is canceled, close stays pending until both cancellation-insensitive cleanup tails are released, then `.closed` may publish, and the flush caller cannot write afterward. Frontend parse and scoped whitespace checks pass; no SwiftPM command, production edit, commit, or unrelated edit ran.
- 2026-08-11T14:58:40Z explicit-flush-bounded-exact-production: `explicit_flush_production` owns `InstantMutationTransport.swift`, the shared explicit/automatic limit types in `BoundedOutboxDelivery.swift`, only the bounded explicit-claim/shared selector hunks in `SQLitePersistenceStore.swift`, and only explicit-flush ownership/deadline/renewal/close-idle hunks in `InstantRuntime.swift`. The prior exact-close and query-once Runtime sections are frozen and must remain unchanged. Root corrected one provisional test expectation before production edits: when an abort races with a valid late response, exact token disposition wins non-interruptibly before timeout, cancellation, or close completes; the response must not be discarded into a duplicate resend. This worker will run parser, scoped diff, and caller scans only; no SwiftPM or commit.
- 2026-08-11T15:22:11Z explicit-flush-bounded-exact-production-source-freeze: production is source-frozen. Public generic `InstantAbortableOperation<Value>` owns one async run plus a synchronous cross-copy idempotent abort, latches abort-before-run so no late I/O can start, and releases run/abort captures at exact exit; `InstantMutationTransportOperation` is its response typealias, legacy `InstantMutationTransportClient` remains cancellation-cooperative (including public `send` reassignment), and `.local`/`preparedOperations` use the exact boundary. Explicit selection delegates to the automatic metadata-first body-at-a-time transaction with one exclusive 50-mutation / 256-step / 50-decode / 8-MiB window, stops at confirmed barriers, preserves bounded quarantine output for actor/lifecycle publication, and carries unchanged selected bodies through confirmation without a second decode while still decoding a concurrently rebased body. Runtime owns one exact explicit operation before selection; deadline, close, and caller cancellation synchronously abort transport but retain renewal until a late response is dispositioned, renewal false/error aborts as a causal race, every transport/deadline/renewal/disposition/watchdog child is canceled and joined, conflicting or over-50 result sets are rejected, and the reporting-only five-second cleanup watchdog never abandons work. Close request-stops then joins explicit work while the operation gate is free before publishing `.closed`; the aggregate idle probe and close watchdog name explicit flush. `limit: 0` remains a read-only empty result and no detached late finisher remains. Combined frontend parse, scoped whitespace, stale-symbol, and caller/API scans pass; no SwiftPM or commit. Hashes: transport `1573e3603825dd6e3d3bf22e6744a4576701da8bf85d7fe362a8082c595e0b93`; limits `d19ba486d88a703b6da7f7d525c1f0ff6c78ad2595cb79cd0c93fe0cef09b3b8`; persistence `3b75ba36e0c03dde18af0bcfdba78be4a7e46142575cf7be929230230c355fd3`; Runtime `6005c16ed1936ccf1ae424440db6f1662abe235ce98ba39bb58e3cf73f230eae`; frozen tests `53f2046890ee3d86a7a17cd01bbe2d1084fb2d235286b247aa8af9b34b107055`.
- 2026-08-11T11:03:23-0400 storage-lifecycle-exact-tests: root delegates `storage_lifecycle_tests` only the focused storage lifecycle contracts in `InstantStorageRuntimeTests.swift` and `InstantStorageHTTPParityTests.swift`. The earlier `streaming_storage_upload` owner is source-frozen and inactive; its file-backed upload assertions remain untouched. This mower slice adds deterministic red contracts for synchronous upload abort plus exact cancellation-insensitive join, no post-cancel local persistence or progress publication, bounded newest progress state, and exact stored-file observation cleanup/buffering where the current test seams support them. Every outer wait is cancellation-cooperative and exactly 5,000 milliseconds. Test files only; frontend parse and scoped whitespace checks; no SwiftPM, production edit, commit, or unrelated edit.
- 2026-08-11T15:16:18Z live-queue-bounds-tests: root delegates `live_queue_bounds_tests` only the new `InstantLiveQueueBoundsTests.swift` contract suite plus this claim/channel bookkeeping. The mower slice requires disconnected stream appends to replay from durable SQLite, 10,000 durable fragments to hydrate in pages of at most 256 fragments / 1 MiB raw / 8 MiB encoded with zero resident payload afterward, reconnect handshake reentrancy not to overwrite a concurrent append, and room broadcast FIFO to preserve A/B/C while A is blocked and reject the newest item at exact per-room count, per-room byte, and process-global byte limits without persistence or observer side effects. Every outer wait is cancellation-cooperative and exactly 5,000 milliseconds. Tests only; frontend parse and scoped whitespace checks; no SwiftPM, production edit, commit, fixed sleep, or unrelated edit.
- 2026-08-11T15:27:07Z live-queue-bounds-tests-source-freeze: the new four-test contract suite is frozen at SHA-256 `acfa2365acadc02f23d8d7ee40707adf600e73039e44f343d56c51ea1aae2fd4`. `disconnectedStreamAppendIsReplayedFromDurableSQLiteOnReconnect` blocks before replacement-session installation, persists an append, and requires its exact wire replay plus zero resident fragment/raw/encoded payload. `tenThousandDurableStreamFragmentsReplayWithinEveryPageAndReleaseResidentPayload` seeds SQLite in one transaction, drains without retaining wire bodies, and requires 10,000 ordered fragments in exactly forty pages whose maxima are 256 fragments, 1 MiB raw, and no more than 8 MiB encoded, followed by zero residency. `reconnectHandshakeReentrancyCannotOverwriteConcurrentDurableAppend` appends during a suspended `start-stream` acknowledgement and requires exactly one send in that generation plus replay on the next offset-zero reconnect. `roomBroadcastQueuePreservesFIFOAndRejectsNewestAtEveryExactBoundWithoutSideEffects` blocks A, proves D cannot overtake queued B/C, then separately fills the exact count, per-room encoded-byte, and process-global encoded-byte bounds and requires newest rejection before SQLite or an active observer sees it. Expected package seams are `InstantLiveStreamReplayLimits`, fixed-counter `liveStreamWriterReplayMetricsForTesting(streamID:)`, `onLiveStreamWriterReplayCompletedForTesting`, `InstantLiveRoomBroadcastQueueLimits` including canonical `encodedByteCount`, `liveRoomBroadcastQueueResidencyForTesting(room:)`, and `onRoomTopicSnapshotPublishedForTesting`. Every outer wait routes through exactly 5,000 milliseconds; there are no real sleeps. Strict `swift-format` lint, one frontend parse, tracked whitespace, and both untracked no-index whitespace checks pass; no SwiftPM command, production edit, commit, or unrelated edit ran.
- 2026-08-11T11:19:34-0400 storage-lifecycle-exact-tests-source-freeze: focused storage lifecycle contracts are frozen. `uploadProgressCancellationAbortsAndExactlyJoinsTransport` requires `InstantStorageTransportOperation<Value>` plus `.preparedOperations(upload:delete:)`, a managed `uploadFileProgressLease`, synchronous one-shot abort, exact wait for a cancellation-insensitive late transport response, no post-cancel local persistence or success/error publication, and release of the completed transport graph while the cancelled lease remains retained; its weak probe is explicitly extended through the late transport response so optimizer lifetime shortening cannot weaken the contract. `storedFilesNewestSnapshotAndExactLiveCancellation` requires a managed `observeStoredFilesLease`, newest-one complete-snapshot buffering, idempotent concurrent `cancelAndWait`, exact Store/live-query cleanup, and a post-merge cancellation fence that prevents a blocked late snapshot from yielding. Approved nil-by-default scalar hooks are `onStoredFilesRemoteSnapshotMergedForTesting(Int)` immediately after merge acquisition and before the cancellation check/yield, plus `onStoredFilesRemoteSnapshotPublishedForTesting(Int)` only after an accepted yield; neither hook can retain a snapshot. Existing `cancelledUploadDoesNotPersist` now uses a cooperative transport and an exact 5,000-millisecond join instead of fixed delay. A distinct newest-two progress policy test is intentionally omitted because the current stream exposes exactly loading plus one terminal state, so oldest-two and newest-two are not distinguishable without widening the testing API; the exact-cancel contract still proves no late terminal is retained. `InstantManagedStreamLease<Stream: Sendable>` is expected to expose `stream` and idempotent `cancelAndWait`. Every outer wait is cancellation-cooperative and exactly 5,000 milliseconds; no fixed sleep, SwiftPM command, production edit, public-test file, commit, or unrelated edit ran. Combined frontend parse and scoped whitespace checks pass. Runtime test SHA-256 `abdc7c777f7e0d62d1c16a98b825f545520ec3fc11e788fd155bb950f578d086`; prior file-backed HTTP parity file remains untouched by this slice at `bf23505718839d15077eb155d8dfe12bf9fb28710d76967a74c947963f97fd8f`.
- 2026-08-11T15:23:28Z storage-lifecycle-exact-production: `storage_lifecycle_production` owns `InstantStorageTransport.swift`, new `InstantManagedStreamLease.swift`, exact storage lease additions in `InstantSnapshotObservers.swift`, storage-only hunks in `InstantRuntime.swift`, and storage-only bridges in `InstantSwiftData.swift`. It preserves every concurrent Runtime/client/observer hunk. Scope is a real synchronous storage-operation abort, reusable idempotent exact managed stream cancellation, newest-two upload progress with no artificial sleep or late persistence/success, compensation when cancellation wins after remote upload, newest-one stored-file observation with weak mapping and exact source join, and runtime-backed Fetch cleanup through the exact lease. No close integration, test edit, SwiftPM command, commit, or unrelated edit.
- 2026-08-11T15:28:59Z live-queue-bounds-tests-global-admission-refreeze: root's pre-production review strengthened the same new suite, now frozen at SHA-256 `7e7d5773be3109e9fbb2009d9e3475e9687e886b2cd11ac2c66a77bcf4cc011a`. `InstantLiveRoomBroadcastQueueLimits` must now include `maximumMessageCountGlobally` as well as a default `maximumEncodedBytesPerMessage == 262_144`. The global-count contract queues thirty-two tiny broadcasts across thirty-two distinct unjoined rooms while remaining far below the global byte ceiling, then requires the thirty-third room's newest item to fail before persistence/publication with unchanged global residency. The single-message contract starts from an empty queue whose aggregate room/global byte budgets can admit the payload, proves its canonical encoded size exceeds 256 KiB, and requires rejection solely at the per-message ceiling with zero persistence, publication, or residency. Existing FIFO, per-room count, aggregate per-room byte, and aggregate global byte cases remain. Strict `swift-format` lint, frontend parse, tracked whitespace, and untracked no-index whitespace checks pass; no SwiftPM command, production edit, commit, fixed sleep, or unrelated edit ran.

- 2026-08-11T16:05:58Z grok-build-wrap-perf claims suite-green+commit+latency-execute under plan 2026-08-11-suite-green-and-latency; preserves concurrent freezes; no production timeout raises.
- 2026-08-11T16:45:00Z grok-build-wrap-perf-fixtures (mower): pure test/doc fixture prep from /tmp/instant-full-suite-20260811-e.log without full SwiftPM. Touches InstantReactorParityTests (two-entity supersession-safe pending-cleanup), InstantLiveTransportTests (intermittent known issue for async encoding quarantine), InstantDiagnosticFeedbackLoopTests + ScribeShapedAuthenticatedIdleMemorySoakTests (≤256-step BoundedBenchmarkSeed splits). Leaves concurrent freezes intact; no commit; no production timeout raises.
- 2026-08-12T12:36:00-0400 user-explicit-allow steal: `grok-4.6-live-revision` steals InstantStore/Runtime ownership for the live-put rematerialize memory slice only. Steal is allowed only because the user explicitly allowed it at this timestamp. Prior 019fe994 ids remain. Scope: Swift-only memory-byte tests, then skip rematerialize of observers whose result does not include the changed entity. Do not touch outbox, deferred values, leases, or bounded-query candidate enumeration. InstantRuntime claimed with no edit unless InstantStore publish contract requires it.
- 2026-08-12T13:07:02-0400 grok-4.6-live-revision splice: InstantStore now skips disjoint include namespaces, splices lastValues when the changed entity is already in the result, and records skipped/spliced/rematerialized counts on `store.mutation-published`. InstantRuntime still unedited. Autoresearch run `2026-08-12-live-put-observation-memory`.
